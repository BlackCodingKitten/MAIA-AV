
\section{Overview of the Evaluated Models}
\subsection*{Qwen Family}
The Qwen models considered in this work share an omnimodal architecture designed to process heterogeneous information within a unified computational framework. Both Qwen2.5-Omni and Qwen3-Omni accept textual, visual, and acoustic inputs, with modality-specific encoders transforming raw signals into representations compatible with a shared language-model backbone. Multimodal integration takes place after perceptual encoding, and particular attention is paid throughout the family to the temporal coordination of audio and video \cite{qwen25omni,qwen3omni}.
\subsubsection*{Qwen2.5-Omni-3B}
Qwen2.5-Omni introduces the \textit{Thinker-Talker} architecture, separating multimodal understanding from speech generation. The \textit{Thinker} processes the input modalities and produces textual representations and responses, while the \textit{Talker} takes the Thinker's internal representations and generates speech from them. Perceptual understanding and spoken generation thus remain functionally distinct components of an otherwise end-to-end pipeline \cite{qwen25omni}.
Text is processed through the Qwen tokenizer. Audio is resampled to 16~kHz and converted into a 128-channel mel-spectrogram, with the encoder producing representations at a temporal resolution of roughly 40~ms. Visual information is handled by a Vision Transformer derived from Qwen2.5-VL and shared across image and video inputs; frames are sampled dynamically to preserve relevant visual content while matching the temporal granularity of the acoustic stream \cite{qwen25omni}.
The modality-specific encoders are limited to perceptual extraction: their output is passed to the \textit{Thinker}, where multimodal information is processed within the shared Transformer. Both audio and visual encoders additionally support block-wise processing, allowing long multimodal sequences to be handled incrementally \cite{qwen25omni}.
Multimodal alignment relies on \textit{Time-aligned Multimodal Rotary Position Embedding} (TMRoPE), which represents positional information along temporal, vertical, and horizontal dimensions. Text is assigned identical positional values across all three; images retain a constant temporal coordinate while preserving spatial position along height and width; audio receives temporal positions tracking its progression through the acoustic stream \cite{qwen25omni}.
Video combines both axes: each frame keeps its two-dimensional spatial layout while carrying a temporal coordinate tied to its position in the clip, on a scale coordinated with the acoustic representation. Audio and video can thus be placed within a common temporal reference system, which is what allows the \textit{Thinker} to relate acoustic and visual evidence occurring at corresponding moments. Encoded representations from both streams are arranged within a single multimodal sequence and processed jointly by the language model's attention mechanism, so integration occurs before response generation rather than through a late combination of independent modality-specific predictions \cite{qwen25omni}.
The \textit{Talker} is a dual-track autoregressive Transformer decoder, conditioned on the high-dimensional representations it receives from the \textit{Thinker}; the resulting discrete speech units are converted into a waveform through a sliding-window diffusion Transformer \cite{qwen25omni}. The technical report describes this organization at the level of the Qwen2.5-Omni family as a whole, without a fusion mechanism specific to the 3B variant \cite{qwen25omni}.
\subsubsection*{Qwen3-Omni-30B-A3B-Instruct}
Qwen3-Omni keeps the Thinker-Talker organization of its predecessor while redesigning several components. It handles text, images, audio, and video, and supports both textual and spoken generation; in the 30B-A3B configuration, both the \textit{Thinker} and the \textit{Talker} adopt a Mixture-of-Experts architecture, increasing overall capacity while limiting the parameters activated at each inference step \cite{qwen3omni}.
The \textit{Thinker} still carries out multimodal understanding and text generation by combining representations from dedicated perceptual encoders. The \textit{Talker}, however, is more loosely coupled to it than in Qwen2.5-Omni: rather than depending directly on the Thinker's high-level textual hidden states, it conditions speech generation on textual context together with high-dimensional visual and acoustic representations \cite{qwen3omni}.
Audio is processed by the \textit{Audio Transformer} (AuT). Raw audio is resampled to 16~kHz and represented as a 128-channel mel-spectrogram; convolutional downsampling brings the resulting representations to roughly 12.5~Hz, or about 80~ms per acoustic frame. AuT uses attention windows of different sizes to support both global audio understanding and block-wise processing \cite{qwen3omni}.
Visual input is processed by an encoder derived from Qwen3-VL and initialized from SigLIP2-So400m, shared across images and video; as in Qwen2.5-Omni, frames are sampled dynamically to preserve visual content while matching the temporal granularity of the audio stream \cite{qwen3omni}.
TMRoPE is retained for multimodal alignment, again along temporal, vertical, and horizontal dimensions: text keeps identical coordinates across all three, images hold a constant temporal coordinate while preserving spatial position, and audio now receives absolute temporal identifiers at roughly 80~ms resolution \cite{qwen3omni}. Video frames are assigned temporal identifiers taken from their actual timestamps, on the same scale used for audio, so acoustic and visual representations share an absolute temporal coordinate system while spatial information is still carried separately through height and width \cite{qwen3omni}.
This replaces the fixed temporal chunking of Qwen2.5-Omni with alignment based on absolute temporal identifiers, producing a finer correspondence between acoustic and visual events and removing the dependence on a predefined chunk size \cite{qwen3omni}. The aligned representations are then processed by the Mixture-of-Experts \textit{Thinker}, so multimodal integration occurs within the shared Transformer computation after perceptual encoding and temporal alignment; the pre-training strategy reinforces this from the outset, drawing on unimodal and cross-modal data spanning image-text, audio-text, video-text, video-audio, and video-audio-text combinations \cite{qwen3omni}.
Speech generation is likewise redesigned. The \textit{Talker} predicts a multi-codebook acoustic representation based on residual vector quantization: the first codebook is generated autoregressively, while a Multi-Token Prediction module estimates the remaining residual codebooks. The final waveform is produced by \textit{Code2Wav}, a causal convolutional renderer built for incremental synthesis \cite{qwen3omni}.
\subsection*{Gemma Family}
The Gemma models considered in this work implement multimodality through two distinct strategies. Gemma 3n E4B relies on dedicated perceptual encoders that convert acoustic and visual signals into representations processed by the language-model backbone. Gemma 4 12B instead adopts a unified, encoder-free architecture, mapping raw perceptual information directly into the decoder's embedding space through lightweight projection modules \cite{gemma3n2025,gemma4technicalreport}.
\subsubsection*{Gemma 3n E4B IT}
Gemma 3n accepts text, images, audio, and video and generates textual responses; separate perceptual encoders convert acoustic and visual signals into token representations for the central language model \cite{gemma3n2025}.
The E4B configuration is built on \textit{MatFormer} (Matryoshka Transformer), which nests a smaller E2B computational path within the larger E4B structure; both are trained jointly, allowing the model to operate under different computational budgets without requiring separate weights \cite{gemma3n2025}.
Gemma 3n also introduces \textit{Per-Layer Embeddings}, storing a substantial portion of the embedding parameters outside accelerator memory. As a result, E4B keeps only around four billion core parameters resident on the accelerator despite a larger overall parameter count ,a gain in computational efficiency rather than a change to how modalities are integrated \cite{gemma3n2025}.
Audio is processed by an encoder based on the Universal Speech Model, producing roughly one acoustic token every 160~ms (about six tokens per second) that feed directly into the language model \cite{gemma3n2025}. Visual input is handled by MobileNet-V5-300M, supporting resolutions of $256 \times 256$, $512 \times 512$, and $768 \times 768$ pixels across both image and video content \cite{gemma3n2025}. A \textit{Multi-Scale Fusion VLM adapter} processes the resulting visual representations before they reach the language model; its role is limited to combining features at different scales and does not amount to a general audiovisual fusion mechanism \cite{gemma3n2025}.
Multimodal integration remains centered on the language-model backbone: audio and visual encoders produce modality-specific representations that become available to the Transformer during generation, and the available documentation does not describe a shared temporal alignment mechanism between acoustic and visual signals comparable to TMRoPE in the Qwen models \cite{gemma3n2025}.
Gemma 3n further employs \textit{KV Cache Sharing}, letting keys and values from intermediate attention layers be reused by subsequent ones, which reduces redundant computation during the prefill stage, a benefit that is particularly relevant for the long sequences produced by audio and video input \cite{gemma3n2025}.
\subsubsection*{Gemma 4 12B IT}
Gemma 4 is a natively multimodal family built on a decoder-only Transformer, and the 12B configuration departs from the rest of the family architecturally. Rather than relying on perceptual encoders, it is trained from scratch under a unified encoder-free paradigm, with visual and acoustic input mapped directly into the language model's embedding space through lightweight projection modules \cite{gemma4technicalreport}.
This sets it apart from the other Gemma 4 configurations: the smaller E2B and E4B models retain distinct vision and audio encoders, and even the larger variants keep dedicated visual processing. Gemma 4 12B alone drops both the vision and the audio encoder, replacing them with a direct projection of raw perceptual information \cite{gemma4technicalreport}.
For visual input, the model operates on RGB patches of $48 \times 48 \times 3$ values. A large linear projection of roughly 35 million parameters takes the place of the Vision Transformer used elsewhere in the family, mapping the resulting representations directly into the language model's embedding space \cite{gemma4technicalreport}. Spatial information is preserved by adding two-dimensional coordinate-based positional embeddings to the projected patches, followed by a final LayerNorm before the visual representations enter the backbone, enough to keep spatial structure available without a separate visual encoder \cite{gemma4technicalreport}.
The audio pathway follows the same principle. Raw audio, sampled at 16~kHz, is divided into 40~ms segments of 640 values each; the USM-based Conformer encoder used by the other Gemma 4 variants is removed entirely, and each 640-dimensional segment is projected directly into the language model's embedding space \cite{gemma4technicalreport}. No additional positional encoding is introduced for these acoustic representations, since their sequential order already preserves the temporal structure of the audio stream. Visual and acoustic information thus enter the same embedding space through modality-specific linear projections rather than through independent high-capacity encoders \cite{gemma4technicalreport}.
Multimodal integration accordingly takes place directly inside the decoder-only Transformer: the projection modules establish representational compatibility between raw perceptual input and the language model's embedding space, and the Transformer itself carries out the subsequent interaction between perceptual and linguistic information, narrowing the separation between perceptual encoding and language processing found in more conventional multimodal systems \cite{gemma4technicalreport}.
The 12B model is dense and combines local and global self-attention, with five local attention blocks per global block. Local layers use RoPE, while global layers use $p$-RoPE with $p=0.25$; the architecture also reuses keys as values in global attention layers, reducing the memory footprint of the global KV

\section{Preliminary Analysis as a Diagnostic Tool for Multimodal Assessment}
The preliminary analysis aims to determine which information relevant to the subsequent resolution of \textit{MAIA-AV} questions can be autonomously recovered by each model from the visual content of the video alone. The analysis is accordingly conducted in a \textit{question-independent} setting: the model receives solely the video input, with no access to the questions, corresponding answers, audio streams, or transcriptions. The output is a structured and temporally grounded representation of the visual content, which is only later compared with the informational requirements of individual questions.
This separation distinguishes the model's capacity to extract visual information from its ability to retrieve relevant evidence when guided by a linguistic query. While the preliminary analysis does not directly measure performance on the \acs{VSV} task, its purpose is to characterize what the model can represent prior to the introduction of a specific question-answering objective.
Isolating these two abilities addresses a key limitation of evaluations based solely on final accuracy. A correct prediction does not guarantee that the model has correctly identified relevant entities, reconstructed events, or established the relations necessary to support its output. Conversely, an incorrect answer fails to reveal which stage of the process caused the failure. The preliminary analysis thus introduces an intermediate diagnostic layer between the raw video content and the final task outcome.
Methodologically, this approach relates to \textit{VILMA} \cite{kesen2024vilma}, specifically regarding its distinction between proficiency tests and main tests. In \textit{VILMA}, proficiency tests evaluate fundamental capabilities deemed necessary for resolving complex temporal phenomena via a zero-shot caption--foil paradigm. While our method does not replicate this specific evaluation protocol, it adopts the same underlying principle: complex performance must be interpreted relative to the prerequisite capabilities that underpin it.
In \textit{MAIA-AV}, these prerequisites are examined through a structured representation of the visual content. For each video, four multimodal and omnimodal models, subsequently evaluated on the \acs{VSV} task, independently analyze temporally localized video segments. Outputs are initially kept separate to enable cross-representation comparison without prematurely merging predictions. Inter-model agreement is interpreted as an indicator of \textbf{prediction stability} rather than absolute ground truth, since multiple models may converge on an identical yet unsupported interpretation.
The representation is constructed progressively across three levels: \textbf{entities}, \textbf{events}, and \textbf{relations between events}. This hierarchy separates directly observable perceptual information from information that requires temporal integration or higher-level inference.
The \textbf{entity level} describes elements identified within the visual content and their observable properties, detailing the objects, individuals, and other scene components detected by the model together with their corresponding temporal intervals of presence.
The \textbf{event level} captures actions, interactions, and state changes tied to specific temporal intervals. Because overlapping video segments may yield duplicate descriptions of the same event, extracted outputs are consolidated to eliminate redundancy while preserving genuinely recurrent events. Temporal anchoring maintains a direct link between each event and the video segment providing its supporting evidence.
The \textbf{relation level} organizes extracted events according to temporal and inferential dependencies. Temporal relations encompass configurations such as \textit{before}, \textit{after}, \textit{during}, and \textit{overlaps}, supporting event reordering and the tracking of changes across the video timeline. Causal relations are treated independently of temporal succession: simple temporal precedence does not by itself establish a cause-and-effect relationship. The analysis accordingly differentiates causal information directly supported by visual evidence from relations that require further inference over events, states, or intentions, keeping observable content separate from information introduced by the model's own inferential process.
The distinction between representational levels matters because questions assigned to the same nominal category can demand substantially different types and volumes of evidence. Identifying an object's location within a single frame, for instance, requires fundamentally different operations than determining its position following a sequence of movements. Similarly, recognizing an isolated event differs from reconstructing a sequence of multiple events or inferring causal links between them.
To capture these distinctions systematically, questions are analyzed along three operational dimensions: \textbf{spatial}, \textbf{temporal}, and \textbf{causal}. A unified three-level scale is applied across each dimension. Level~0 denotes cases where the required information can be directly retrieved. Level~1 involves integrating an explicit relation or a limited number of evidence units. Level~2 demands broader reconstruction across multiple events, temporal phases, state changes, or implicit relations. These criteria are summarized in Table~\ref{tab:question_classifications}.
This classification reflects the informational requirements of the questions rather than their linguistic complexity, accounting for properties such as the temporal distribution of evidence, the number of relevant evidence units, the presence of inter-event relations, and whether the required information is explicit or implicit.
Once constructed independently, the visual representation can be compared against the question requirements to determine whether the entities, events, and relations necessary to answer a question are already captured within the vision-only representation. The evaluation therefore does not depend directly on the model's capacity to generate the correct answer. Instead, it assesses whether the supporting information is present within its visual representation.
This comparison is particularly relevant for \textit{MAIA-AV}, since it establishes a visual baseline prior to introducing additional modalities. Prior findings \cite{deodati2026lost} showed that, in the original \textit{MAIA} setting, incorporating acoustic information yielded no systematic performance gains, while including transcriptions led to a slight yet statistically robust decline in accuracy, consistent with a dataset originally designed around visual content rather than information explicitly conveyed via audio.
The preliminary analysis thus establishes \textit{what can be seen} prior to evaluating the contribution of \textit{what can be heard}. It clarifies whether the visual channel inherently contains the required evidence and highlights categories where visual information alone proves insufficient, so that the incremental contributions of audio and transcriptions can be assessed against an explicit visual baseline rather than solely through shifts in downstream task accuracy.
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{immagini/preliminar_analysis_schema.png}
    \caption{Conceptual schema of the preliminary analysis.}
    \label{fig:preliminar_analysis_schema}
\end{figure}
Overall, the preliminary analysis transforms \textit{MAIA-AV} from a purely result-driven evaluation into a diagnostic, competence-oriented framework. Final performance can thus be analyzed across specific sub-capabilities, including entity recognition, event identification, temporal integration, spatial reconstruction, and causal inference, disentangling evidence availability failures from errors in downstream selection or integration during the \acs{VSV} task.
\begin{table}[H]
    \centering
    \small
    \renewcommand{\arraystretch}{0.9}
    \begin{tabularx}{\textwidth}{|l|X|X|X|}
        \hline
        \rowcolor{EcoForestGreen}
        \textcolor{white}{\textbf{Category}} &
        \textcolor{white}{\textbf{Level 0}} &
        \textcolor{white}{\textbf{Level 1}} &
        \textcolor{white}{\textbf{Level 2}} \\
        \hline
        \cellcolor{EcoForestGreen}
        \textcolor{white}{\textbf{Spatial (S)}} &
        Direct retrieval of an entity's location or spatial property from a single frame. &
        Explicit spatial relation or event-conditioned localization involving at most two evidence units. &
        Spatial reconstruction across multiple phases, including movement, relocation, or state change. \\
        \hline
        \cellcolor{EcoForestGreen}
        \textcolor{white}{\textbf{Temporal (T)}} &
        Recognition of a single event or state without temporal comparison. &
        Temporal localization or pairwise ordering between two explicit events. &
        Multi-event temporal reconstruction, including duration, repetition, onset, or state evolution. \\
        \hline
        \cellcolor{EcoForestGreen}
        \textcolor{white}{\textbf{Causal (C)}} &
        Retrieval of an explicitly stated cause without inference. &
        Explicit cause-and-effect relation involving at most two evidence units. &
        Implicit or multi-event causal reconstruction requiring contextual or intentional inference. \\
        \hline
    \end{tabularx}
    \caption{Complexity levels for spatial, temporal, and causal questions.}
    \label{tab:question_classifications}
\end{table}
\subsection*{Video Preprocessing: Temporal Segmentation and Frame Sampling}
Prior to the entity extraction phase, each video is preprocessed by dividing it into four-second temporal windows with a stride of three seconds. Consecutive spans share one second of visual content. This overlap reduces the risk that an action or state change occurring near the boundary between two segments is only partially observed, while maintaining shared context across consecutive observations without altering the chronological order of the video.
For each segment, the \texttt{start\_time} and \texttt{end\_time} are preserved, keeping each segment associated with its original temporal position in the video. This information is maintained throughout all subsequent stages of the pipeline, allowing observations to be reconnected with their exact position in the video.
The windowing process serves exclusively to provide temporal indexing and structure visual evidence, leaving content and semantic interpretation to subsequent phases. Redundant information is deliberately retained at this stage to prioritize continuity of evidence, with redundancy resolution deferred to the output consolidation step. The preprocessing phase ultimately yields an ordered sequence of temporally localized visual units that serve as the shared foundation for all subsequent stages of analysis.
\paragraph{Implementation Details} The preprocessing stage is implemented in \texttt{video\_pre-\\processing.py} to convert each source video into a set of temporally indexed visual representations. The script first retrieves core metadata via \texttt{ffprobe}, including duration, frame rate, spatial resolution, codec, and audio stream presence. Although audio metadata is recorded, the acoustic signal is excluded from visual representation construction. Frames are extracted using OpenCV by seeking directly to target temporal positions, and each is saved as a JPEG image with its original timestamp embedded in both the filename and the JSON manifest, preserving temporal provenance independently of downstream processing.
A core output of the script is the dense visual stream, sampled by default at 4 FPS:
\begin{lstlisting}[language=Python]
parser.add_argument("--dense-fps", type=float, default=4.0)
\end{lstlisting}
Extracted frames are saved in the \texttt{dense\_frames} directory for each video. Simultaneously, the script generates 32 uniform global frames and performs shot-boundary detection via PySceneDetect. These boundaries define variable-length segments with a target duration of 4 seconds, ranging from 2 to 6 seconds, and a default overlap of 0.5 seconds, extracting eight frames per segment. The preprocessing output is serialized into \texttt{metadata.json}, \texttt{frames.json}, \texttt{shots.json}, and \texttt{segments.json}.
For the subsequent preliminary analysis, semantic extraction scripts operate directly on the dense stream rather than shot-based segments. Utility functions locate each video's \texttt{dense\_frames} directory, parse timestamps from filenames, and sort frames chronologically, so that the model receives temporally ordered visual observations instead of isolated or semantically segmented images.
The inference temporal windows are configured as follows:
\begin{lstlisting}[language=Python]
parser.add_argument("--window-seconds", type=float, default=4.0)
parser.add_argument("--stride-seconds", type=float, default=3.0)
\end{lstlisting}
A 4-second window with a 3-second stride yields a 1-second overlap between consecutive observations. The pipeline selects dense frames within these nominal boundaries, and if frame counts exceed the allowed maximum, frames are sampled uniformly across the window. At 4 FPS, a 4-second interval contains 16 frames, remaining well below the default 32-frame ceiling. Duplicate consecutive windows are automatically omitted.
Each temporal unit is encapsulated in a \texttt{TemporalWindow}, mapping a unique segment ID to its start time, end time, and frame paths. Recorded boundaries reflect the timestamps of the first and last selected frames rather than nominal window limits, with millisecond precision maintained to ensure exact alignment with the source video.
The transition to semantic extraction relies strictly on \textit{ordered dense frames}. For each window, the model receives selected RGB images alongside a prompt specifying the video ID, temporal interval, and frame-to-timestamp map, with no questions, answers, transcriptions, or acoustic signals provided. Temporal segmentation thus serves purely as an indexing mechanism for visual evidence, deferring semantic interpretation to the subsequent extraction stage.
\subsection*{Semantic Representation Extraction}
Building upon the defined temporal windows, the pipeline proceeds with semantic representation extraction. This stage generates a structured semantic description for each window, processing every window independently. The extraction procedure is repeated across all four evaluated models. Although the models employ distinct architectures and inference modalities, each receives identical inputs and is constrained to produce a standardized representation adhering to a common schema, as shown in Table~\ref{tab:semantic_representation}. Consequently, performance differences can be attributed directly to model capabilities rather than annotation formatting.
For each window, the representation spans seven semantic categories:
\begin{description}
\item[\textbf{entities}]: Operating at the most fundamental descriptive level, each entity receives a local identifier, a concise label, visually observable attributes, and its role within the scene when discernible. The representation tracks the temporal occurrence of the entity and the specific frames providing visual evidence. These local identifiers enable consistent references across other descriptive categories within the same segment.
\item[\textbf{actions}]: Actions represent visually observable occurrences, specifying the performing agent and target object whenever feasible.
\item[\textbf{events}]: Events denote broader semantic units linked to one or more participants across a given temporal span. This distinction separates the detection of an isolated action from the broader event context in which it occurs.
\item[\textbf{spatial\_relations}]: Spatial configurations between entities or scene elements are defined using a subject-relation-object structure. Permissible relations include configurations such as \textit{in front of}, \textit{behind}, \textit{above}, \textit{below}, \textit{inside}, \textit{outside}, \textit{left of}, \textit{right of}, \textit{near}, and \textit{on}. These spatial descriptors are temporally localized, meaning a spatial configuration observed in one segment is not assumed to hold throughout the video.
\item[\textbf{state\_changes}]: State changes capture observable transformations affecting an entity. By contrasting a prior state with a subsequent state, this structure represents temporal variations within a segment. This level is essential when answering a question requires tracking changes in an entity's properties, position, or condition over time.
\item[\textbf{temporal\_relations}]: The model describes temporal links such as \textit{before}, \textit{after}, \textit{overlaps}, \textit{during}, \textit{starts}, \textit{finishes}, and \textit{simultaneous}. These relations, however, remain strictly localized to the current window: global video temporal structure is not reconstructed at this stage, nor are event correspondences across different segments systematically resolved.
\item[\textbf{causal\_hypotheses}]: Causal candidate relations are explicitly separated from directly observed facts. The framework requires causal hypotheses to be flagged as inferred evidence, preventing temporal precedence from being automatically interpreted as a cause-and-effect relationship, and keeping a clear boundary between observed evidence and model inference.
\end{description}
The distinction between observed and inferred evidence is preserved through the \textit{evidence\_type} field, accompanied by a confidence score between 0 and 1. Each element also includes an \textit{evidence\_frames} list identifying supporting frames. During output normalization, frame references are retained only if they fall within the processed window, and generated temporal intervals are similarly bounded by segment limits, keeping semantic representations strictly aligned with their supporting visual evidence.
Categories may remain empty if a model detects insufficient evidence within a segment. At the segment level, the pipeline preserves \textit{segment\_id}, \textit{start\_time}, \textit{end\_time}, and \textit{input\_frames}.
At this initial stage, a unified global video description is not yet constructed. Because consecutive windows overlap, identical entities, actions, or events may be extracted repeatedly across adjacent segments. This redundancy is intentionally preserved during extraction to maintain a direct mapping between each prediction and its supporting local evidence, leaving entity reconciliation and global temporal aggregation to subsequent pipeline stages.
\begin{table}[H]
\centering
\small
\renewcommand{\arraystretch}{0.9}
\begin{tabularx}{\textwidth}{|l|X|}
    \hline
    \rowcolor{EcoForestGreen}
    \textcolor{white}{\textbf{Field}} &
    \textcolor{white}{\textbf{Information represented}} \\
    \hline
    \texttt{entities} &
    Identified entities, observable attributes, roles, and temporal occurrence. \\
    \hline
    \texttt{actions} &
    Local actions, their actors, and involved objects. \\
    \hline
    \texttt{events} &
    Temporally extended local events and their participants. \\
    \hline
    \texttt{spatial\_relations} &
    Spatial configurations between entities or scene elements. \\
    \hline
    \texttt{state\_changes} &
    Observable transitions between an entity's previous and subsequent states. \\
    \hline
    \texttt{temporal\_relations} &
    Temporal relations between events identified within the same local context. \\
    \hline
    \texttt{causal\_hypotheses} &
    Candidate causal relations explicitly marked as inferred evidence. \\
    \hline
\end{tabularx}
\caption{Semantic information extracted from each temporal window.}
\label{tab:semantic_representation}
\end{table}
\paragraph{Implementation Details}
Entity and semantic analysis is implemented through a unified extraction pipeline shared across all evaluated models. Model-specific wrapper scripts load the corresponding architectures and format selected input frames, while temporal framing, prompt generation, schema definitions, validation logic, and serialization are centralized in \texttt{utils.py}, so that every model is evaluated against an identical semantic protocol regardless of underlying architectural differences.
The common semantic space is defined across seven categories:
\begin{lstlisting}[language=Python]
SEMANTIC_CATEGORIES = (
    "entities",
    "actions",
    "events",
    "spatial_relations",
    "state_changes",
    "temporal_relations",
    "causal_hypotheses",
)
\end{lstlisting}
The categories illustrated in Table~\ref{tab:semantic_representation} specify the structured information required from each temporal window.
For each temporal window, the pipeline constructs a standardized extraction prompt via \texttt{build\_semantic\_prompt}, which supplies the video ID, window boundaries, and an explicit frame-to-timestamp map alongside the category definitions:
\begin{lstlisting}[language=Python]
frame_map = "\n".join(
    f'- frame {index}: "{path.name}", '
    f'timestamp={get_timestamp(path):.3f}s'
    for index, path in enumerate(
        window.frame_paths,
        start=1,
    )
)
\end{lstlisting}
Rather than eliciting free-form descriptions, the prompt\footnote{English translation: \textit{"Do not invent details that are not visible. Use evidence\_type="observed" for directly visible facts. Use evidence\_type="inferred" only for unavoidable inferences or causal hypotheses. For each element, indicate confidence between 0 and 1 and the exact frame names that support it. Do not confuse temporal succession with a causal relationship. If a category contains no elements, return an empty list. Return exclusively a valid JSON object."}} enforces strict visual grounding: every extracted element must reference supporting frames, temporal boundaries, a confidence score, and an \texttt{evidence\_type}. Directly visible details are classified as \texttt{observed}, whereas inferential claims are labeled as \texttt{inferred}, and the prompt explicitly instructs the model not to treat simple temporal succession as causality:
\begin{lstlisting}[language=Python]
    """Non inventare dettagli non visibili.
    Usa evidence_type="observed"
    per fatti direttamente visibili.
    Usa evidence_type="inferred"
    soltanto per inferenze inevitabili
    o ipotesi causali.
    Per ogni elemento indica confidence
    tra 0 e 1 e i nomi esatti dei frame
    che lo supportano.
    Non confondere una successione
    temporale con una relazione causale.
    Se una categoria non contiene elementi,
    restituisci una lista vuota.
    Restituisci esclusivamente
    un oggetto JSON valido."""
\end{lstlisting}
Models are constrained to output structured JSON objects containing standardized metadata, including \texttt{start\_time}, \texttt{end\_time}, \texttt{evidence\_frames}, \texttt{evidence\_type}, and \texttt{confidence}. Entity identifiers generated within a window can be cross-referenced across actions, events, and relations in the same segment.
The main execution loop is encapsulated in \texttt{process\_video\_with\_inferencer}. After dense frames are partitioned into temporal windows, each window is processed sequentially:
\begin{lstlisting}[language=Python]
for window_index, window in enumerate(
    windows,
    start=1,
):
    prompt = build_semantic_prompt(
        video_directory.name,
        window,
    )
    response = inferencer(
        window.frame_paths,
        prompt,
    )

    parsed = parse_model_json(response)

    segment = normalize_semantic_output(
        parsed,
        window,
    )

    segments.append(segment)
\end{lstlisting}
The \texttt{inferencer} component is the sole model-dependent step in this loop, accepting frame paths and prompts and returning textual output. All downstream parsing and validation operations remain identical across architectures.
To manage output formatting inconsistencies from generative models, the parsing stage strips Markdown wrappers and extracts the primary JSON object. If standard decoding fails, the pipeline attempts recovery using \texttt{json\_repair} before marking the response invalid:
\begin{lstlisting}[language=Python]
try:
    parsed = json.loads(candidate)
except json.JSONDecodeError as first_error:
    try:
        from json_repair import repair_json
        parsed = json.loads(
            repair_json(candidate)
        )
    except Exception as repair_error:
        raise SemanticOutputError(
            f"JSON non valido: {first_error}"
        ) from repair_error
\end{lstlisting}
Structural constraints are subsequently enforced by \texttt{normalize\_semantic\_output}, which clips temporal boundaries to window limits, restricts confidence scores to the interval $[0,1]$, and filters out invalid frame references. Evidence types are validated against \texttt{observed}, \texttt{inferred}, or \texttt{uncertain}, with causal hypotheses automatically reclassified as \texttt{inferred} if initially marked as \texttt{observed}:
\begin{lstlisting}[language=Python]
confidence = _safe_float(
    item.get("confidence"),
    0.0,
)
item["confidence"] = round(
    max(0.0, min(1.0, confidence)),
    4,
)
evidence_type = str(
    item.get(
        "evidence_type",
        "observed",
    )
).lower()
if evidence_type not in {
    "observed",
    "inferred",
    "uncertain",
}:
    evidence_type = "uncertain"
if (
    category == "causal_hypotheses"
    and evidence_type == "observed"
):
    evidence_type = "inferred"
\end{lstlisting}
Frame-level grounding validation retains only filenames present in the input temporal window:
\begin{lstlisting}[language=Python]
valid_evidence = set(
    window.evidence_frames
)
item["evidence_frames"] = [
    str(name)
    for name in evidence_frames
    if str(name) in valid_evidence
]
\end{lstlisting}
Normalized segments are enriched with temporal metadata from the preprocessing stage:
\begin{lstlisting}[language=Python]
normalized = {
    "segment_id": window.segment_id,
    "start_time": window.start_time,
    "end_time": window.end_time,
    "input_frames": window.evidence_frames,
}
\end{lstlisting}
Outputs are saved for each video in a \texttt{\_semantic.json} file alongside model metadata and window configuration settings. Window failures are logged in an \texttt{errors} field containing segment identifiers, temporal bounds, frame lists, and error descriptions, preserving optional raw responses for diagnostic review.
This phase ultimately transforms temporally indexed visual evidence into structured local representations. Cross-window deduplication is deliberately omitted at this step to preserve local grounding, leaving element consolidation and full video reconstruction to subsequent analysis stages.
\subsection*{Event Analysis}
\subsubsection*{Spatial Relations}
Spatial information captures entity configurations within the scene and tracks positional changes across localized visual observations. Unlike temporal relations, spatial features are extracted directly during the semantic representation phase, while visual windows are still accessible.
Spatial relations encode explicit configurations between entities, or between an entity and a scene element. These include positional, proximity, containment, and orientation relations such as \textit{left of}, \textit{right of}, \textit{above}, \textit{below}, \textit{inside}, \textit{outside}, \textit{in front of}, \textit{behind}, \textit{near}, and \textit{on}. Because these descriptors are tied to localized segments, a single entity pair may exhibit distinct spatial arrangements across different temporal intervals.
Displacements and state transitions also drive spatial variation. A positional change is represented by combining the spatial relations observed at different timestamps with the corresponding action and any recorded \texttt{state\_changes}, distinguishing static scene layouts from dynamic rearrangements. Event-dependent localization further evaluates spatial configurations relative to an event interval, before, during, or after, isolating how a prior action alters an entity's final position.
Methodologically, co-occurrence does not imply a spatial relation. The presence of two entities within the same temporal segment provides insufficient grounds to assume proximity, orientation, containment, or contact: a relation is retained only when explicitly captured in the semantic analysis. Spatial evidence currently remains bound to these local semantic descriptions. The event extraction stage draws on \texttt{spatial\_relations} for event reconstruction but does not construct a global spatial graph.
\subsubsection*{Temporal Relations}
Temporal information is formalized during event extraction, where local semantic descriptions are consolidated into a chronological timeline covering the full video. Each consolidated event retains a \texttt{start\_time} and an \texttt{end\_time} defining its temporal boundary and, implicitly, its duration.
Relations between events are modeled explicitly through pairs of \texttt{event\_id} references. Supported relation types are \textit{before}, \textit{after}, \textit{overlaps}, \textit{during}, and \textit{simultaneous}, each linking two existing events in the consolidated representation together with an associated confidence score. \textit{Before} and \textit{after} express temporal precedence and sequence, \textit{during} denotes temporal containment, \textit{overlaps} indicates partial interval sharing, and \textit{simultaneous} marks concurrent occurrences.
This structure captures chronological succession, repetition, containment, and overlap across the video timeline while keeping distinct occurrences separate. Generation order within the JSON output does not itself constitute temporal evidence: dependencies derive strictly from segment timestamps and relational metadata rather than from the model's output sequence.
The temporal schema adopts the following structure:
\begin{lstlisting}[language=Python]
"temporal_relations": [
{
"first_event": "E0001",
"relation": "before|after|overlaps|during|simultaneous",
"second_event": "E0002",
"confidence": 0.0
}
]
\end{lstlisting}
This consolidated event structure also serves as input for the subsequent causal analysis, though chronological organization is kept distinct from causal inference: temporal precedence does not by itself establish a cause-and-effect relationship.
\paragraph{Implementation Details}
The event analysis is implemented as a second-stage transformation over the structured semantic representations produced by the previous phase. No visual input is processed again at this stage. For each model, the corresponding semantic output is loaded and used as the only source of information for event reconstruction. The event analyzer can reorganize and consolidate previously extracted information, but it cannot introduce evidence obtained from a new inspection of the video, preserving the separation between perceptual extraction and subsequent structural reasoning.
The input therefore consists of the temporally indexed semantic segments generated for the same model. Each segment retains its identifier and temporal boundaries together with the semantic categories extracted from the corresponding visual window. The information made available to the event analysis includes entities, actions, local events, spatial relations, state changes, temporal relations, and causal hypotheses:
\begin{lstlisting}[language=Python]
keys = (
"segment_id",
"start_time",
"end_time",
"entities",
"actions",
"events",
"spatial_relations",
"state_changes",
"temporal_relations",
"causal_hypotheses",
)
\end{lstlisting}
These fields are considered jointly because an event may not be represented exclusively in the local \texttt{events} field. An action or an observable state transition, for instance, may provide relevant information for reconstructing an event even when the local semantic description does not encode it as an independent event. The event analysis therefore operates over the complete local semantic representation rather than simply concatenating the previously extracted event lists.
Before inference, the semantic segments are arranged according to their temporal position and serialized into a structured textual input. The model is explicitly instructed to treat this representation as a closed source of evidence, with the common prompt constraining the reconstruction process through a set of rules designed to prevent additional perceptual or commonsense information from being introduced:
\begin{lstlisting}[language=Python]

* usa esclusivamente le informazioni presenti
  nell'analisi semantica
* non inventare azioni, oggetti, intenzioni,
  emozioni o cause mancanti
* unisci soltanto i duplicati causati dalla
  sovrapposizione delle finestre
* mantieni separati eventi realmente distinti
  o ripetuti
* ricava i tempi soltanto dai segmenti forniti
\end{lstlisting}
A central function of this phase is the consolidation of local descriptions produced by overlapping temporal windows. Since the semantic extraction uses 4-second windows with a 3-second stride, adjacent segments share part of the same visual evidence, and the same physical event can consequently appear in multiple semantic outputs. During event reconstruction, descriptions referring to the same occurrence are merged into a single event, an operation intended to remove redundancy introduced by the preprocessing strategy rather than repetitions that genuinely occur in the video.
The prompt explicitly enforces this distinction. Events with similar descriptions are not automatically treated as duplicates: two occurrences are merged only when their temporal context and supporting segments indicate that they represent the same event observed through overlapping windows. Repeated actions occurring at different moments, by contrast, remain represented as distinct events, so that repetition is preserved as a meaningful temporal property of the video.
The consolidated representation follows a common JSON schema. Each reconstructed event contains a unique identifier, a concise description, temporal boundaries, the participants involved, the semantic segments supporting the event, the type of evidence, and a confidence value:
\begin{lstlisting}[language=Python]
{
"event_id": "E0001",
"description": "string",
"start_time": 0.0,
"end_time": 0.0,
"participants": ["entity or participant"],
"evidence_segments": ["segment_0001"],
"evidence_type":
"observed|inferred|uncertain",
"confidence": 0.0
}
\end{lstlisting}
The \texttt{event\_id} field provides a stable reference for subsequent relational analyses. Events are assigned progressive identifiers such as \texttt{E0001}, \texttt{E0002}, and \texttt{E0003}, associated with the consolidated chronological representation rather than inherited from the local semantic outputs, so that events can be referred to consistently once multiple local descriptions have been merged.
Temporal localization is inherited from the semantic analysis, since the event extractor does not estimate event boundaries by accessing the original frames. Instead, \texttt{start\_time} and \texttt{end\_time} must remain compatible with the temporal intervals of the semantic segments supporting the event. When the same occurrence is represented across multiple overlapping windows, its consolidated interval can reflect the temporal evidence distributed across those segments.
The \texttt{evidence\_segments} field preserves provenance after consolidation. An event extracted from a single semantic window may contain only one segment identifier, whereas an event observed across overlapping windows may be associated with several:
\begin{lstlisting}[language=Python]
"evidence_segments": [
"segment_0004",
"segment_0005"
]
\end{lstlisting}
Consolidation removes duplicated event descriptions without discarding the evidence from which they originated, keeping the resulting event representation traceable to the local observations produced in the previous phase, even though it is more compact than the semantic representation itself.
Participants are derived from entities or participants already represented in the semantic analysis and are not generated as independent new entities at this stage, so that the event representation preserves semantic continuity between entity extraction and event reconstruction.
The epistemic status of each event is represented through \texttt{evidence\_type}. The analysis distinguishes three values:
\begin{lstlisting}[language=Python]
"observed"
"inferred"
"uncertain"
\end{lstlisting}
\texttt{Observed} is used for events supported by information previously represented as directly visible. \texttt{Inferred} is reserved for information that already requires an inferential interpretation of the available semantic evidence. \texttt{Uncertain} represents cases in which the available evidence does not support a stronger classification. The event extraction stage is not intended to transform directly observed information into unsupported inference.
Confidence is represented numerically in the $[0,1]$ interval and subsequently normalized to ensure a consistent representation. Normalization also verifies the structural validity of the generated output before serialization.
In addition to individual events, the same inference produces an initial global temporal organization. Temporal relations connect events through their newly assigned identifiers rather than through their textual descriptions:
\begin{lstlisting}[language=Python]
"temporal_relations": [
{
"first_event": "E0001",
"relation":
"before|after|overlaps|during|simultaneous",
"second_event": "E0002",
"confidence": 0.0
}
]
\end{lstlisting}
The allowed relations distinguish precedence, succession, containment, partial temporal overlap, and simultaneity, providing an explicit representation of the temporal structure that emerges once local events have been consolidated.
Temporal relations cannot be derived solely from the order in which events are written in the generated JSON. The prompt explicitly requires temporal organization to depend on the intervals and relations available in the semantic representation: the order in which the text is generated is not treated as temporal evidence, and temporal succession is likewise kept distinct from causal interpretation.
The model output is parsed as structured JSON rather than retained as free-form text. Responses are cleaned of possible Markdown delimiters, and the JSON object is extracted before decoding. The same robust parsing strategy used in the semantic stage allows malformed but recoverable outputs to be repaired when possible:
\begin{lstlisting}[language=Python]
text = (
text.replace("`json", "")
        .replace("`", "")
.strip()
)

start = text.find("{")
end = text.rfind("}")

if start < 0 or end < start:
raise ValueError(
"La risposta non contiene un oggetto JSON"
)

payload = json.loads(
text[start:end + 1]
)
\end{lstlisting}
This phase primarily changes the granularity of the representation. The semantic analysis describes evidence locally within individual overlapping windows, while the event analysis reorganizes those observations into a coherent event-based representation of the complete video, removing redundancies caused by temporal overlap, preserving genuinely repeated occurrences, assigning stable identifiers, maintaining temporal and evidential provenance, and establishing the first explicit relations among events, all without any new inspection of the visual input.
\subsection*{Causal Inference}
Causal inference operates exclusively on the consolidated event representation, independently of perceptual extraction. This phase receives no visual frames or raw multimodal input, relying solely on previously reconstructed events and their established \texttt{temporal\_relations}.
Isolating causal reasoning in this way prevents the recovery of omitted visual details or the insertion of unobserved events. Before processing, the event representation is pruned to its essential fields: \texttt{event\_id}, description, temporal interval, participants, supporting semantic segments, \texttt{evidence\_type}, and confidence.
The analysis considers four types of causal dependency:
\begin{itemize}
  \item \texttt{causes}: one event directly produces the outcome represented by another.
  \item \texttt{enables}: one event establishes the conditions required for another to occur.
  \item \texttt{motivates}: the available evidence supports an intentional or motivational link between events.
  \item \texttt{prevents}: one event inhibits or counteracts another.
\end{itemize}
Each entry links a \texttt{cause\_event} to an \texttt{effect\_event}, optionally supplemented by \texttt{supporting\_events} where additional events are needed to substantiate the inference. No new events are introduced during this phase.
The resulting structure follows this schema:
\begin{lstlisting}[language=Python]
"causal_relations": [
  {
    "cause_event": "E0001",
    "relation": "causes|enables|motivates|prevents",
    "effect_event": "E0002",
    "evidence_type": "direct|inferred|uncertain",
    "supporting_events": ["E0003"],
    "explanation": "string",
    "confidence": 0.0
  }
]
\end{lstlisting}
The epistemic status of each relation is indicated by \texttt{evidence\_type}. Dependencies grounded in explicit physical interactions or state transitions are labeled \texttt{direct}. Connections that require contextual interpretation are labeled \texttt{inferred}, while partially supported links receive \texttt{uncertain}. Motivational dependencies require explicit support within the event representation rather than speculative plausibility.
Temporal succession does not imply causation: a chronological link such as \textit{before} or \textit{after} remains insufficient on its own to establish a causal relation. Dependencies are introduced only when the semantic content explicitly supports causation, enabling, motivation, or prevention. Keeping these stages separate ensures that temporal ordering is never mistaken for causal evidence, preserving the distinction between extracted facts and inferential reasoning.
\paragraph{Implementation Details}
The causal inference stage is a text-only processing step applied to the consolidated event representation produced in the preceding phase. For each video, the pipeline loads the corresponding \texttt{\_events.json} file generated by the model. No raw video frames, audio signals, transcriptions, or downstream text prompts are supplied, so the causal analyzer operates exclusively on a closed representation of previously extracted visual information.
Prior to inference, the event file is pruned by the \texttt{compact} function, which retains only the fields required for causal reasoning:
\begin{lstlisting}[language=Python]
def compact(payload):
    return {
        "id_video": payload.get("id_video"),
        "events": [
            {
                "event_id": event.get("event_id"),
                "description": event.get("description"),
                "start_time": event.get("start_time"),
                "end_time": event.get("end_time"),
                "participants": event.get("participants", []),
                "evidence_segments": event.get("evidence_segments", []),
                "evidence_type": event.get("evidence_type"),
                "confidence": event.get("confidence"),
            }
            for event in payload.get("events", [])
        ],
        "temporal_relations": payload.get("temporal_relations", []),
    }
\end{lstlisting}
This filtering isolates causal reasoning from the original perceptual features, requiring the model to derive dependencies strictly from the consolidated events and their temporal layout. The allowable relation types and evidence categories are bounded explicitly in code:
\begin{lstlisting}[language=Python]
ALLOWED_RELATIONS = {
    "causes",
    "enables",
    "motivates",
    "prevents",
}
ALLOWED_EVIDENCE = {
    "direct",
    "inferred",
    "uncertain",
}
\end{lstlisting}
These definitions separate direct cause-and-effect links (\texttt{causes}) from enabling conditions (\texttt{enables}), intentional drivers (\texttt{motivates}), and inhibitory actions (\texttt{prevents}).
Inference is guided by a standardized prompt that instructs the model to rely solely on the provided events and temporal relations, explicitly forbidding it from treating simple chronological succession as causal evidence:
\begin{lstlisting}[language=Python]

* usa esclusivamente gli eventi e le relazioni
temporali presenti nell'input
* non usare domande, caption, foil o altre
informazioni esterne
* la semplice successione temporale NON implica causalita'
* crea una relazione soltanto quando il contenuto
degli eventi supporta una dipendenza causa-effetto,
una condizione abilitante, una motivazione oppure
una prevenzione
* cause_event ed effect_event devono essere ID
di eventi presenti nell'input
* non inventare eventi, oggetti, intenzioni o cause
non ricavabili dalla rappresentazione fornita
* se non esiste evidenza sufficiente, restituisci
causal_relations come lista vuota
\end{lstlisting}
Within this schema, relationships are classified by epistemic confidence: \texttt{direct} for explicit physical or state-change interactions, \texttt{inferred} for contextual or intentional deductions, and \texttt{uncertain} for weakly supported cues. Output formatting enforces the same structured JSON schema used elsewhere in the pipeline:
\begin{lstlisting}[language=Python]
{
"causal_relations": [
{
    "cause_event": "E0001",
    "relation": "causes|enables|motivates|prevents",
    "effect_event": "E0002",
    "evidence_type": "direct|inferred|uncertain",
    "supporting_events": ["E0003"],
    "explanation": "string",
    "confidence": 0.0
}
]
}
\end{lstlisting}
The fields \texttt{cause\_event} and \texttt{effect\_event} reference existing event IDs, while \texttt{supporting\_events} optionally logs contextually relevant secondary events.
Output parsing strips Markdown delimiters and decodes the outer JSON object. If formatting errors occur, \texttt{json\_repair} attempts structural recovery, and if parsing fails entirely, a secondary generation call requests strictly formatted JSON without altering the input prompt:
\begin{lstlisting}[language=Python]
text = text.replace("json", "").replace("", "").strip()
start = text.find("{")
end = text.rfind("}")
candidate = text[start : end + 1]
try:
    return json.loads(candidate)
except json.JSONDecodeError:
    from json_repair import repair_json
    return json.loads(repair_json(candidate))
\end{lstlisting}
A normalization step then applies a sequence of structural checks before the relations are accepted. It first collects the set of valid event identifiers parsed from the input:
\begin{lstlisting}[language=Python]
valid_ids = {
    event.get("event_id")
    for event in payload.get("events", [])
    if event.get("event_id")
}
\end{lstlisting}
Candidate relations that reference an unknown event, or that link an event to itself, are discarded:
\begin{lstlisting}[language=Python]
if (
    cause not in valid_ids
    or effect not in valid_ids
    or cause == effect
):
    continue
\end{lstlisting}
Relations using an unrecognized type are dropped, while evidence labels outside the allowed set default to \texttt{inferred}:
\begin{lstlisting}[language=Python]
if rel not in ALLOWED_RELATIONS:
    continue
if evidence not in ALLOWED_EVIDENCE:
    evidence = "inferred"
\end{lstlisting}
Relations sharing the same cause, relation, and effect are then deduplicated, keeping only the first occurrence of each triplet:
\begin{lstlisting}[language=Python]
key = (cause, rel, effect)
if key in seen:
    continue
seen.add(key)
\end{lstlisting}
Supporting-event references are filtered in the same way, excluding invalid identifiers as well as the cause and effect events themselves:
\begin{lstlisting}[language=Python]
supporting = [
    event_id
    for event_id in relation.get("supporting_events", [])
    if event_id in valid_ids and event_id not in {cause, effect}
]
\end{lstlisting}
Finally, confidence scores are clipped to the $[0, 1]$ interval:
\begin{lstlisting}[language=Python]
try:
    confidence = float(relation.get("confidence", 0.0))
except (TypeError, ValueError):
    confidence = 0.0
confidence = max(0.0, min(1.0, confidence))
\end{lstlisting}
Final outputs are serialized per video into \texttt{\_causal.json} files containing metadata, model parameters, and the resulting relations. Unresolvable errors yield empty relation arrays rather than speculative fallbacks.
\subsection*{NLI-Based Visual Proficiency Assessment}
The final stage of the preliminary analysis integrates the information produced at the semantic, event, temporal, spatial, and causal levels into a consolidated representation of the visual evidence recovered by each model. The procedure operates exclusively on the outputs generated during the preceding phases, preserving the separation between visual analysis and question-dependent evaluation without performing any further inspection of the video.
The integrated representation combines complementary information produced at different levels of the pipeline. Entity descriptions, spatial relations, and state changes originate from the semantic analysis. Consolidated events provide a global event-level representation together with their participants, temporal boundaries, supporting segments, evidence type, and confidence. Temporal relations connect these events through their identifiers, while causal relations encode the dependencies established during causal inference. These components are treated not as independent observations, but as progressively structured views of the same visual evidence.
Finalization does not amount to a simple concatenation of all intermediate outputs. Redundancies introduced by overlapping temporal windows have already been resolved during event consolidation, where multiple descriptions of the same occurrence are merged while genuinely repeated events remain distinct, and duplicated or structurally invalid causal relations have already been removed during causal normalization. The epistemic status assigned during the previous stages is preserved as well: information classified as \texttt{observed}, \texttt{inferred}, or \texttt{uncertain} remains distinguishable in the consolidated representation, together with the corresponding confidence values. Uncertain information is not automatically promoted to positive evidence, nor are conflicting descriptions resolved through an additional interpretation introduced at this stage. The final representation thus captures the entities, events, spatial configurations, temporal organization, and causal dependencies recovered by the model, without expanding the evidence beyond what the visual analysis actually produced.
Crucially, \textbf{finalization does not constitute an answer to any \acs{VSV} question}. Its purpose is to fix a representation of what the model has recovered from the visual modality before the linguistic content of the evaluation item is introduced. Questions, captions, and foils therefore play no role in constructing the representation, and once it has been finalized, its content remains fixed throughout the evaluation of the corresponding question: the question may provide context for interpreting the linguistic alternatives, but it cannot trigger the extraction of new entities, events, relations, or visual evidence.
The proficiency assessment itself is formulated as a Natural Language Inference task. The finalized visual representation acts as the \textbf{premise}, while the caption and the corresponding foil are evaluated independently as alternative hypotheses. GPT-4o is employed exclusively as an NLI judge, not to answer the original question, generate a new description of the video, or select directly between the two alternatives. Its role is restricted to determining the logical relation between the evidence contained in the finalized representation and each hypothesis.
For both caption and foil, the judge assigns one of three labels: \texttt{entailment}, \texttt{contradiction}, or \texttt{neutral}. \texttt{Entailment} indicates that the hypothesis is supported by the available visual representation. \texttt{Contradiction} indicates that it is incompatible with the represented evidence. \texttt{Neutral} is used when the available information is insufficient to establish either relation, so that missing evidence is not interpreted as contradiction and absence of information is not treated as negative evidence.
The caption and foil are judged separately. A caption--foil pair receives \texttt{PASS} only when the finalized representation entails the caption and contradicts the foil. Every other combination produces \texttt{NOT PASS}.
This rule adopts a deliberately conservative criterion. Entailment of the caption alone is insufficient, since the same representation must also provide enough evidence to reject the minimally contrastive foil. Contradiction of the foil, in turn, cannot compensate for a caption classified as \texttt{neutral}. A model is therefore considered proficient on a given pair only when its visual representation supports both sides of the required distinction.
The procedure is repeated independently for the four caption--foil pairs associated with each question. Question-level proficiency requires consistency across the complete set: a question receives \texttt{PASS} only when all four pairs satisfy the pair-level criterion, and is classified as \texttt{NOT PASS} if at least one pair fails. This strict aggregation reduces the influence of individual linguistic formulations and requires the recovered visual evidence to remain sufficient across multiple, minimally different realizations of the same underlying item.
For each pair, the procedure records the classification assigned to the caption, the classification assigned to the foil, and the resulting binary decision, which are then aggregated into the final proficiency label associated with the question. The resulting measure is accordingly distinct from conventional question-answering accuracy: rather than evaluating whether the model can generate or select the correct answer after being exposed to the question, it evaluates whether the information independently recovered from the visual channel is sufficiently complete and discriminative to support the correct statement while rejecting its minimally contrastive alternative, connecting question-independent visual analysis to the competence-oriented evaluation objective of the preliminary analysis.
\paragraph{Implementation Details}
The final evaluation is implemented in \texttt{final\_evaluation.py}, which operates on the finalized representations produced for the four evaluated models. These representations are stored as JSON files in \texttt{data/preliminar\_analysis/final\_results} and constitute the fixed visual premises used throughout the proficiency assessment. The script expects one finalization file for each model and verifies their presence before starting the evaluation:
\begin{lstlisting}[language=Python]
EXPECTED_MODELS = (
"gemma",
"gemma-4-12B",
"qwen",
"qwen-30B",
)

FINAL_DIR = Path(
"data/preliminar_analysis/final_results"
)

final_files = [
FINAL_DIR / f"{model}.json"
for model in EXPECTED_MODELS
]
\end{lstlisting}
Finalized representations are loaded independently for each model and indexed by normalized video identifiers. The loading procedure accepts different top-level JSON organizations, including lists of video representations, dictionaries containing \texttt{videos} or \texttt{results}, individual video objects, and dictionaries directly indexed by video ID, allowing the subsequent NLI procedure to retrieve the complete representation associated with a video without modifying its content.
Question metadata and caption--foil pairs are loaded separately from \texttt{question\_classification.csv} and \texttt{caption\_foil.csv}. Video identifiers and question categories are normalized before the two sources are joined on the video and principal question dimension. The question is therefore introduced only after the finalized visual representation has been loaded, and is retained as contextual information for the NLI judge rather than incorporated into the premise itself.
The NLI evaluation uses \texttt{gpt-4o-2024-08-06} with deterministic generation:
\begin{lstlisting}[language=Python]
MODEL = "gpt-4o-2024-08-06"
risposta = client.chat.completions.parse(
model=MODEL,
temperature=0,
messages=[
{
"role": "system",
"content":
f"{PROMPT}\n\n{REGOLE[categoria]}",
},
{
"role": "user",
"content": contenuto,
},
],
response_format=RispostaNLI,
)
\end{lstlisting}
Each request contains three distinct fields: the question, the finalized representation used as the premise, and one hypothesis. The caption and foil are never evaluated jointly. Instead, two independent calls are performed using the same premise and question context:
\begin{lstlisting}[language=Python]
nli_caption = classifica(
premessa,
row["domanda"],
row["categoria"],
row["caption"],
)

nli_foil = classifica(
premessa,
row["domanda"],
row["categoria"],
row["foil"],
)
\end{lstlisting}
The system prompt explicitly restricts the judge to the information contained in the premise. External knowledge, completion of missing information, and plausibility-based reasoning are excluded, and incomplete, weak, or ambiguous evidence must be classified as \texttt{neutro}. Additional constraints are introduced according to the question category: spatial relations cannot be inferred from simple co-occurrence, temporal relations cannot be inferred from the textual order of events, and causal relations cannot be established from temporal succession alone.
The model response is constrained through a Pydantic schema rather than parsed from unrestricted natural-language output:
\begin{lstlisting}[language=Python]
class RispostaNLI(BaseModel):
etichetta: Literal[
"implicazione",
"contraddizione",
"neutro",
]
\end{lstlisting}
This design restricts each inference to one of the three admissible NLI labels, preventing the judge from generating explanations or free-form answers. A missing or structurally invalid classification raises an error rather than being converted into an evaluation decision.
Pair-level proficiency is subsequently computed through a deterministic rule implemented outside GPT-4o:
\begin{lstlisting}[language=Python]
def valuta_coppia(caption, foil):
return (
"PASS"
if caption == "implicazione"
and foil == "contraddizione"
else "NOT PASS"
)
\end{lstlisting}
GPT-4o's role is thus limited to assigning the two NLI labels: it does not directly decide whether a proficiency test is passed. The binary outcome is derived programmatically, receiving \texttt{PASS} only when the finalized representation entails the caption and contradicts the corresponding foil. All other label combinations result in \texttt{NOT PASS}.
Results are written incrementally to a model-specific checkpoint in the \texttt{nli} subdirectory. Before evaluating a pair, the script checks whether its identifier has already been processed, allowing interrupted executions to resume without repeating completed API calls. Each stored row contains the model, pair identifier, video, category, question, caption, foil, the two NLI classifications, and the resulting pair-level outcome.
Once all model-specific evaluations have been completed, pair-level records are concatenated into \texttt{nli\_risultati.csv}. Question-level proficiency is then computed by grouping results according to model, video, category, and question:
\begin{lstlisting}[language=Python]
df = risultati.groupby(
[
"modello",
"video_id",
"categoria",
"domanda",
]
).agg(
totale_coppie=("esito", "size"),
coppie_pass=("pass", "sum"),
).reset_index()

df["proficiency"] = [
"PASS" if passate == totale else "NOT PASS"
for passate, totale in zip(
df["coppie_pass"],
df["totale_coppie"],
)
]
\end{lstlisting}
The aggregation adopts the same conservative principle used at pair level: proficiency is assigned only when every caption--foil pair associated with the question passes. Since the experimental data contain four pairs for each question, this corresponds to the required $4/4$ criterion, and a single \texttt{NOT PASS} is sufficient to classify the question as \texttt{NOT PASS}. The script additionally stores the number of evaluated pairs, the number of successful pairs, and their ratio in \texttt{question\_proficiency.csv}. The implementation keeps the responsibilities of the two components distinct: the finalized JSON representation remains unchanged and functions solely as the visual premise, GPT-4o performs constrained NLI classification over each caption and foil, and the final proficiency decision is produced by deterministic aggregation rules rather than by an additional generative judgment.

\section{Core Reasoning Assessment Framework}
\subsection*{Experimental Logic and Hypotheses}
The experimental design of MAIA-AV extends beyond a simple comparative evaluation of model accuracy. Its primary objective is to analyze the role played by individual modalities in the decision-making process and verify whether correct answers are effectively derived from input evidence.
Experiments are structured around a \textbf{Modality-Ablation Design}. By holding the test item constant while systematically varying the input modalities, it is possible to observe how predictions shift when a specific information source is added or removed. This experimental control enables \textbf{paired comparisons}, allowing behavioral variance to be attributed more precisely to the selected modality configuration.
Furthermore, this setup establishes a clear distinction between \textbf{Modality Sufficiency} and \textbf{Modality Necessity}. A modality is defined as sufficient when it independently contains all the information required to solve the task. However, sufficiency does not imply that the modality is uniquely useful, nor does it preclude the utility of additional sources. Conversely, supplementing a sufficient modality with another does not guarantee performance gains; the added input may be redundant, introduce irrelevant noise, or, in the worst-case scenario, interfere with a previously correct prediction.
Our experimental setup thus shifts the focus from assessing overall model capability toward evaluating how effectively a system integrates available modalities and whether distinct modality preferences emerge. From this perspective arise the concepts of \textbf{Modality Dominance}, \textbf{Modality Gain, Repair, Damage}, and \textbf{Cross-Modal Interaction}, which are formally defined as evaluation metrics in subsequent sections.
This methodological framework is rooted in the principle of \textbf{controlled information removal} established in \textit{Lost in Transcription}~\cite{deodati2026lost}. In both paradigms, the underlying task remains constant while the input information accessible to the model is systematically varied. This setup allows us to directly evaluate whether an added modality compensates for the absence of another or provides an advantage when partial evidence is already available. In \textit{Lost in Transcription}, analysis was primarily oriented toward assessing acoustic contributions within a benchmark built around visual evidence. The main focus was verifying whether the acoustic/textual signal could compensate for absent visual evidence and how this contribution shifted as visual evidence is added.
With MAIA-AV, we extend this paradigm through a more articulated manipulation of input modalities. The framework comprises the following unimodal and baseline conditions: complete absence of input, isolation of the acoustic signal, isolation of visual information, and isolation of audio transcriptions. These initial conditions establish how much relevant information can be retrieved from a single modality alone. We then introduce two multimodal configurations: video with native audio (the full audiovisual clip) and video with audio transcriptions (muted video paired with text transcriptions).
In these multimodal settings, the focus extends beyond whether audio merely assists a primarily visual decision. Instead, the goal is to comprehend how the availability and representation of information across different modalities reshape the model's overall reasoning. This setup transforms the core question from a simplistic inquiry---\textit{does audio help?}---into a broader investigation of how distinct information sources jointly contribute to multimodal reasoning.

\subsection*{Visual Statement Verification}
All experimental conditions adopt an identical task formulation: \textbf{Visual Statement Verification} (VSV). Each decision is evaluated on a pair consisting of a caption (a correct description) and a foil (a minimally contrastive false description). These two alternatives are presented to the model as a binary choice, and the system must return exclusively 0 or 1 depending on its selection. This constrained formulation minimizes confounding factors associated with open-ended text generation, enabling a direct comparison across models and input configurations.
Consequently, the exact same experimental unit can be observed across different multimodal configurations without introducing variations in prompt structure or evaluation criteria. This task structure allows us to distinguish two analytical levels. The first corresponds to single caption-foil choices, designated as \textbf{Item-Level Decisions}. The second is \textbf{Aggregate Accuracy}, which evaluates sets of items linked to the same underlying conceptual category to assess whether the model maintains consistent behavior across different formulations. These metrics are formally defined in subsequent sections.
\begin{table}[h]
    \centering
    \small
    \renewcommand{\arraystretch}{1.35}

    \begin{tabularx}{\linewidth}{|X|X|}
        \hline

        % ================= ITALIANO =================

        \rowcolor{EcoForestGreen!55}
        \multicolumn{2}{|c|}{
            \textcolor{white}{\textbf{Domanda}}
        } \\
        \hline

        \multicolumn{2}{|c|}{
            \parbox{0.94\linewidth}{
                \centering
                Dove cammina la bambina alla fine della scena?
            }
        } \\
        \hline

        \textbf{\textcolor{EcoForestGreen}{Caption}} &
        \textbf{\textcolor{EcoForestGreen}{Foil}} \\
        \hline

        Alla fine della scena, la bambina cammina
        \textbf{su un tappeto}. &
        Alla fine della scena, la bambina cammina
        \textbf{su un sentiero}. \\
        \hline
        % ================= ENGLISH =================

        \rowcolor{EcoForestGreen!55}
        \multicolumn{2}{|c|}{
            \textcolor{white}{\textbf{Question}}
        } \\
        \hline

        \multicolumn{2}{|c|}{
            \parbox{0.94\linewidth}{
                \centering
                Where does the little girl walk at the end of the scene?
            }
        } \\
        \hline

        \textbf{\textcolor{EcoForestGreen}{Caption}} &
        \textbf{\textcolor{EcoForestGreen}{Foil}} \\
        \hline

        At the end of the scene, the little girl walks
        \textbf{on a carpet}. &
        At the end of the scene, the little girl walks
        \textbf{on a path}. \\
        \hline

    \end{tabularx}

    \caption{Example of a minimally contrastive caption--foil pair, reported in Italian with its English translation.}
    \label{tab:minimal-contrastive-pair}
\end{table}

\subsection*{Experimental Setup}

\begin{table}[h]
    \centering
    \small
    \renewcommand{\arraystretch}{1.35}

    \begin{tabularx}{\linewidth}{
        |>{\centering\arraybackslash}p{0.20\linewidth}
        |>{\centering\arraybackslash}p{0.22\linewidth}
        |X|
    }
        \hline

        \rowcolor{EcoForestGreen}
        \textcolor{white}{\textbf{Setup}} &
        \textcolor{white}{\textbf{Input}} &
        \textcolor{white}{\textbf{Description}} \\
        \hline

        \cellcolor{EcoForestGreen!20}
        \textbf{No Input} &
        None &
        The model receives no multimodal information. 
        This condition measures the extent to which the answer can be inferred from linguistic priors or dataset biases. \\
        \hline

        \cellcolor{EcoForestGreen!20}
        \textbf{Audio Only} &
        Audio &
        The model receives only the raw audio track, while the visual content is removed. 
        This setup evaluates the information that can be recovered exclusively from the acoustic modality. \\
        \hline

        \cellcolor{EcoForestGreen!20}
        \textbf{Video Only} &
        Video &
        The model receives only the visual content of the video, without audio. 
        This condition measures how much task-relevant information can be extracted from the visual modality alone. \\
        \hline

        \cellcolor{EcoForestGreen!20}
        \textbf{Audio + Video} &
        Audio + Video &
        The complete audiovisual signal is provided to the model. 
        This represents the fully multimodal condition and allows the contribution of joint audio-visual processing to be assessed. \\
        \hline

        \cellcolor{EcoForestGreen!20}
        \textbf{Transcription + Video} &
        Transcription + Video &
        The visual content is combined with a textual transcription of the audio track. 
        This setup tests whether linguistic information extracted from audio is more effectively exploited when provided explicitly as text. \\
        \hline

    \end{tabularx}

    \caption{Overview of the experimental setups used to evaluate the contribution of different input modalities.}
    \label{tab:experimental-setups}
\end{table}
The protocol is operationalized through a standardized pipeline and a set of adapters tailored to specific families of models. This pipeline centralizes various tasks, including the preparation of items, the definition of experimental conditions, the construction of prompts, the management of inputs, and the storage of outputs.
Inputs consist of caption-foil pairs, original videos, versions without audio tracks, separate audio files, and precomputed transcripts. The pre-established order of alternatives in the dataset is preserved, and the target retains essential information for verifying decision accuracy.
For each item, the raw response generated by the model is also preserved. From this response, the binary label required for the task is subsequently extracted. This mechanism enables the differentiation between valid responses and unparsable outputs, facilitating the documentation of potential execution errors.
A significant distinction concerns modes not supported by the architecture. A lack of support for a specific type of input is not treated as an incorrect response. Instead, such a condition is marked as unavailable, and the model is only excluded from comparisons requiring that specific configuration. Therefore, the protocol should be interpreted through a model-by-mode matrix, where the availability of conditions may vary across different architectures.
\paragraph{Cross-Architecture Input Standardization}The experimental protocol remains uniform across model families, with input delivery adapted to specific multimodal interfaces, including varying processors and formats for Qwen and Gemma. These adaptations ensure identical information payload across different native architectures without altering the experimental design.
\paragraph{Video Processing} The preparation of visual content adheres to a standardized sampling policy. In the experiments, video content is encapsulated using 32 frames evenly distributed across its duration. This strategy ensures that the amount of visual information provided to various architectures remains comparable, while also accommodating the specific preprocessing requirements of the respective processors.
In visual-only conditions, a version of the video devoid of audio components is used. In audiovisual configurations, visual information is coupled with the acoustic tracks specified by the protocol.
Thus, sampling represents a shared experimental choice. Internal differences among the visual encoders of the models fall outside this segment of the protocol and should be addressed separately in the architectural descriptions.
\paragraph{Audio and Transcription Processing} The acoustic component is prepared in a format compatible with the frontend of models capable of processing native audio. When needed, the track is extracted from the video and normalized according to the requirements of the specific adapter. Implementations requiring a 16 kHz signal apply resampling before inference.
The transcription follows a different logic. It is not produced during the main experiment and does not represent an intermediate output of the model under evaluation. Instead, it serves as a precomputed input, kept consistent across the conditions using it and directly integrated into the textual context.
This distinction is methodologically significant. The transcription primarily preserves recognized linguistic content, while the audio signal retains additional acoustic properties. Comparing these two representations allows for the study not just of the presence of information, but also of the form in which it becomes accessible to the model.
\paragraph{Inference Protocol} Inference parameters are configured to minimize stochastic variability. Because the task demands a binary classification choice rather than open-ended text generation, deterministic decoding is enforced using greedy search (e.g., setting \texttt{temperature = 0} or \texttt{do\_sample=False}, depending on the model interface).
Maximum output length is strictly constrained. Restricting generation to a minimal token budget mitigates non-compliant formatting and ensures consistency across models and experimental conditions.
Inference is executed using \texttt{bfloat16} precision where supported. Models are deployed across execution backends, including vLLM and Hugging Face Transformers. These infrastructure settings preserve the underlying experimental logic, while hardware-dependent parameters, such as GPU allocation and framework-specific workarounds, are detailed in the technical appendix for reproducibility.
\subsubsection*{Zero-input Condition}
The no-input experimental condition completely removes perceptual information, providing the model exclusively with the textual caption--foil pair required to complete the task. No video frames, audio signals, or transcriptions of any kind are supplied. This configuration does not assess audiovisual comprehension; rather, its function is strictly diagnostic. It allows us to establish the extent to which decisions can be derived from the linguistic plausibility of the options, structural regularities within the dataset, or systematic parametric biases of the model. Performance significantly above chance level in this condition serves as a strong indicator of potential linguistic shortcuts, operating independently of genuine content understanding.
\paragraph{Implementation Details}
The \textit{No Input} experiment was implemented through a model-independent evaluation pipeline shared across all architectures considered in the study. The purpose of this condition is to remove every source of perceptual information while preserving the binary decision structure of the Visual Statement Verification task. For each item, the model is presented exclusively with the two minimally contrastive textual alternatives. The original question, video frames, audio track, and corresponding transcription are entirely omitted.
The experimental logic is centralized in a common evaluation module, independent from model-specific inference adapters. Each adapter is responsible only for formatting textual requests according to the native interface of its target architecture. This separation ensures that identical items, instructions, target labels, output parsing procedures, and evaluation criteria are applied consistently across all models.
The system instruction is shared across the entire experimental pipeline:
\begin{quote}
\small
\texttt{Sei un assistente binario per un task di Visual Statement Verification.}\\
\texttt{Rispondi esclusivamente con 0 oppure 1.\footnote{English translation: \textit{You are a binary assistant for a Visual
Statement Verification task. Respond exclusively with either 0 or 1.}}}
\end{quote}
The prompt used for the \textit{No Input} condition is likewise identical for all models:
\begin{quote}
\small
\texttt{Basandoti esclusivamente sulle due alternative testuali, scegli.\footnote{English translation: \textit{Based exclusively on the two textual alternatives, choose.}}}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.\footnote{English translation: \textit{Respond exclusively with either 0 or 1.}}}
\end{quote}
Here, \texttt{answer1} and \texttt{answer2} correspond to the two members of the caption--foil pair. Their numerical ordering is independent of their semantic status; dataset target labels indicate whether the correct caption occupies position \texttt{0} or \texttt{1}. The model is thus asked to perform a binary selection between competing linguistic realizations rather than generating open-ended text.
At the implementation level, prompt construction is performed centrally before passing the request to any model adapter:
\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}
In \textit{No Input} mode, no transcription is retrieved and no multimodal content is attached. Consequently, only the formatted textual prompt reaches the model. Although conversation formatting relies on each architecture's native chat template, this implementation detail does not alter the information available to the model.
All models are constrained to produce binary outputs. To handle cases where generative models wrap predictions in extraneous text, responses are normalized via a regex parsing function that extracts the first isolated digit (\texttt{0} or \texttt{1}):
\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(r"(?<!\d)[01](?!\d)", str(text))
    return int(m.group()) if m else None
\end{lstlisting}
If no valid binary label can be recovered, the prediction is flagged as \texttt{invalid} rather than being forcibly mapped to a candidate answer, preventing malformed outputs from skewing accuracy.
The evaluation loop remains uniform across models. For every item, the shared pipeline builds the prompt, invokes the appropriate adapter, parses the response, and checks correctness against the ground-truth target:
\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)
pred = prediction(raw)
result = (
    "correct" if pred == int(row["target"])
    else "wrong" if pred is not None
    else "invalid"
)
\end{lstlisting}
Output records are serialized uniformly across models. Each entry stores item metadata, model ID, experimental condition, raw model output, parsed binary label, and correctness status:
\begin{lstlisting}[language=Python]
FIELDS = [
    "id", "video_id", "video_name", "pool_id",
    "pool_item", "question_category", "question",
    "caption", "foil", "answer1", "answer2",
    "target", "model", "mode", "raw_model_output",
    "predicted_label", "is_correct", "result", "error",
]
\end{lstlisting}
Results are written to model-specific output files for the \textit{No Input} condition. Existing item identifiers are cached prior to execution, allowing interrupted runs to resume seamlessly without recomputing completed instances. Outputs are flushed immediately after writing to prevent data loss.
Inference is executed deterministically across all adapters by disabling stochastic sampling. Disabling sampling is particularly crucial in the \textit{No Input} condition to eliminate output variance unrelated to the underlying linguistic representations.
Overall, model-specific code is strictly limited to weight loading, chat template formatting, and generation execution. Prompt construction, decision formatting, output parsing, correctness evaluation, error handling, and serialization remain completely standardized. This ensures that performance variations in the \textit{No Input} condition reflect differences in model capability rather than inconsistencies in the experimental protocol.

\subsubsection*{Audio-only Condition}
In the \textit{Audio Only} condition, the model exclusively receives the raw acoustic signal, with no visual frames or textual transcriptions provided. This setting allows us to identify which caption-foil pairs can be resolved based on spoken content, environmental sounds, background noise, music, or prosodic speech characteristics. Furthermore, comparing performance in this condition against other experimental configurations reveals whether acoustic information retains its independent utility when combined with visual input, or if its contribution is diminished or overshadowed by another dominant modality. \paragraph{Implementation Details}
The \textit{Audio-Only} condition was implemented as a controlled unimodal setting designed to isolate the contribution of the acoustic channel. For each VSV item, the model receives the raw audio associated with the corresponding video together with the two minimally contrastive textual alternatives. No visual frames, video representations, or audio-derived transcriptions are provided. The original question is retained solely as dataset metadata and is omitted from the inference prompt. Consequently, the model must discriminate between caption and foil by relying on acoustic evidence and the linguistic content of the two candidate statements.
As in the \textit{No Input} condition, experimental logic is centralized in a common evaluation module shared across all four models. Model-specific adapters are responsible only for converting audio and textual content into the representation expected by each architecture. This separation preserves the same prompt, candidate ordering, target labels, prediction parsing, and evaluation procedure across models, while allowing each architecture to process the acoustic signal through its native multimodal interface.
The system instruction remains unchanged across experimental conditions, but
for the \textit{Audio-Only} condition, the following prompt is used:
\begin{quote}
\small
\texttt{Ascolta esclusivamente l'audio fornito.}\\
\texttt{Scegli quale delle due descrizioni è corretta sulla base delle}\\
\texttt{informazioni ricavabili esclusivamente dall'audio.}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
The two alternatives correspond to the caption--foil pair associated with the VSV item. Their numerical positioning does not encode semantic status, as the correct caption may occur in either position. The target field stored in the dataset determines whether the true statement corresponds to label \texttt{0} or \texttt{1}. The model is thus required to perform a binary selection rather than produce an open-ended description.
At the pipeline level, the acoustic resource associated with each item is resolved from its video identifier alongside other media paths:
\begin{lstlisting}[language=Python]
def paths(row):
    stem = Path(row["video_name"]).stem
    return {
        "audio": AUDIO_DIR / f"{stem}.mp3",
        "mute": MUTE_DIR / f"{stem}.mp4",
        "video": VIDEO_DIR / f"{stem}.mp4",
    }
\end{lstlisting}

Only the acoustic component is selected by the model adapter when \texttt{only\_audio} mode is active. The exact conversion of this resource into model input is architecture-dependent. Models accepting audio files directly receive the acoustic resource through their native processor, whereas architectures requiring raw waveforms decode the signal into the format expected by their inference backend. These operations strictly concern input formatting and do not introduce textual transcriptions or visual information.

Prompt construction remains entirely model-independent:
\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}

In the \textit{Audio-Only} condition, the \texttt{transcript} argument remains empty. Thus, the textual portion of the request contains only task instructions and the two competing statements. The acoustic stream constitutes the sole perceptual source available to the model.

The common evaluation loop passes the selected condition, item, formatted prompt, and resolved media paths to the corresponding model-specific inference adapter:
\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)

pred = prediction(raw)
\end{lstlisting}

Regardless of internal generation interfaces, all models are required to return a binary decision. The generated text is normalized through the same shared parsing function used across remaining experimental conditions:
\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(r"(?<!\d)[01](?!\d)", str(text))
    return int(m.group()) if m else None
\end{lstlisting}

The first isolated occurrence of either \texttt{0} or \texttt{1} is interpreted as the prediction. If neither label can be recovered, the output is classified as \texttt{invalid} instead of being forcibly mapped to candidate options. The parsed prediction is subsequently checked against the stored target:
\begin{lstlisting}[language=Python]
result = (
    "correct" if pred == int(row["target"])
    else "wrong" if pred is not None
    else "invalid"
)
\end{lstlisting}

Inference is executed deterministically across model adapters by disabling stochastic sampling. This ensures that variation between predictions is attributable to the information available in the experimental condition rather than sampling variability.

The output structure is likewise shared with other experimental settings. For each item, the pipeline logs the model identifier, experimental condition, raw model generation, parsed binary prediction, target label, correctness status, and potential execution errors. Results are written to a dedicated \texttt{only\_audio} directory, with a separate output file for each model. Completed item identifiers are detected prior to execution, allowing interrupted runs to resume seamlessly without recomputing previously processed instances.

This condition therefore differs from \textit{No Input} solely in the availability of the acoustic signal. This controlled modification allows us to quantify whether speech, environmental sounds, music, or prosodic cues contain sufficient information to discriminate true captions from minimally contrastive foils, independently of visual evidence and audio-derived textual representations.
\subsubsection*{Video-only Condition}
In the \textit{Video Only} condition, the model receives exclusively the visual stream, with the audio track removed. This condition serves as the primary baseline to evaluate which decisions can be sustained purely by visual evidence. Furthermore, the visual condition plays a central role in paired comparisons: subsequently introducing audio or transcriptions enables us to measure the marginal contribution of these additional information sources relative to a baseline where visual context is already available. 
\paragraph{Implementation Details.}
The \textit{Video-Only} condition was implemented as a controlled visual
setting designed to isolate the contribution of the visual modality. For each
VSV item, the model receives the visual content of the corresponding video
together with the two minimally contrastive textual alternatives. The acoustic
track and the audio-derived transcription are excluded from the input. The
original question is retained as dataset metadata but is not included in the
inference prompt. The decision must therefore be based on the visual evidence
available in the video and on the linguistic contrast between caption and foil.
As in the other experimental conditions, the general evaluation procedure is
centralized in a model-independent module. The same prompt structure, candidate
ordering, target labels, prediction parser, and output format are used for all
four models. Model-specific adapters are responsible only for converting the
visual input into the representation required by the corresponding
architecture.
The system instruction remains unchanged across experimental conditions:
For the \textit{Video-Only} condition, the model receives the following
condition-specific prompt:

\begin{quote}
\small
\texttt{Osserva il video fornito.}\\
\texttt{Scegli quale delle due descrizioni è corretta sulla base}\\
\texttt{esclusivamente delle informazioni visive disponibili.}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.\footnote{English translation: \textit{Observe the provided video. Choose which of
the two descriptions is correct based exclusively on the available visual
information. 0: \{answer1\}. 1: \{answer2\}. Respond exclusively with either
0 or 1.}}
}
\end{quote}

The two alternatives correspond to the caption--foil pair associated with the
VSV item. Their numerical position does not encode whether an alternative is
true or false. The correct caption can occupy either position and the target
label stored in the dataset specifies the expected binary prediction. This
prevents the numerical labels from carrying semantic information and preserves
the same decision structure adopted in the other experimental conditions.

To guarantee that no acoustic information can reach the model, the
\textit{Video-Only} condition uses a dedicated muted version of each video.
The common pipeline resolves the available media resources from the identifier
of the corresponding item:

\begin{lstlisting}[language=Python]
def paths(row):
    stem = Path(row["video_name"]).stem
    return {
        "audio": AUDIO_DIR / f"{stem}.mp3",
        "mute": MUTE_DIR / f"{stem}.mp4",
        "video": VIDEO_DIR / f"{stem}.mp4",
    }
\end{lstlisting}

For this condition, only \texttt{paths["mute"]} is passed to the visual
processing stage. The use of a physically muted video rather than simply
instructing the model to ignore audio guarantees that the acoustic channel is
absent from the actual multimodal input.

The visual representation is standardized to a maximum of 32 frames sampled
across the complete temporal extent of the video:

\begin{lstlisting}[language=Python]
N_FRAMES = 32
\end{lstlisting}

Frames are distributed uniformly over the sequence rather than being extracted
from a single local interval. Conceptually, their positions correspond to
equally spaced indices between the beginning and the end of the video:

\begin{lstlisting}[language=Python]
frame_indices = np.linspace(
    0,
    total_frames - 1,
    min(N_FRAMES, total_frames),
    dtype=int,
)
\end{lstlisting}

When a video contains fewer than 32 frames, all available frames are retained.
This sampling strategy provides a fixed visual budget while preserving
information from the complete temporal development of the clip. The extracted
frames are converted to RGB before being supplied to the multimodal processor.
Depending on the native interface of the architecture, the resulting sequence
is subsequently represented either as a video input or as an ordered sequence
of image inputs. This implementation difference is confined to the
model-specific adapter and does not alter the visual evidence available in the
experiment.

Prompt construction remains independent from the model architecture:

\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}

In the \textit{Video-Only} condition, the \texttt{transcript} argument remains
empty. Therefore, the textual component of the request contains only the task
instruction and the two alternatives. No linguistic information derived from
the audio track is introduced.

At inference time, the common evaluation loop combines the prompt with the
media paths and delegates only the architecture-specific processing to the
corresponding adapter:

\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)
pred = prediction(raw)
\end{lstlisting}
All models are required to return a binary output. The generated response is
normalized through the same parser used in the remaining experimental
conditions:
\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(r"(?<!\d)[01](?!\d)", str(text))
    return int(m.group()) if m else None
\end{lstlisting}
The first isolated occurrence of either \texttt{0} or \texttt{1} is interpreted
as the prediction. Outputs from which no valid label can be extracted are
classified as invalid rather than being automatically assigned to one of the
two alternatives. The resulting label is then compared with the target stored
for the corresponding caption--foil pair:
\begin{lstlisting}[language=Python]
result = (
    "correct" if pred == int(row["target"])
    else "wrong" if pred is not None
    else "invalid"
)
\end{lstlisting}
Inference is performed deterministically, with stochastic sampling disabled
across the model adapters. For each item, the pipeline stores the raw
generation, the parsed binary prediction, the target label, the correctness
status, the model identifier, and the experimental condition. Results are
written to a dedicated \texttt{only\_video} directory using a separate output
file for each model.
The \textit{Video-Only} condition therefore preserves the same binary VSV
decision protocol adopted throughout the experimental framework while
restricting perceptual evidence to the visual channel. Its comparison with
\textit{No Input} makes it possible to quantify the contribution introduced by
visual grounding, while comparison with the audio-visual conditions provides
the reference point needed to determine whether additional modalities improve,
preserve, or interfere with visually grounded predictions.
\subsubsection*{Transcription-only Condition}
In the \textit{Transcription Only} condition, the model receives exclusively the textual transcription derived from the audio track. This configuration isolates spoken linguistic content from non-verbal acoustic features, such as prosody, speaker characteristics, background noise, and music. Consequently, this setting evaluates performance driven strictly by explicit verbal information.

\paragraph{Implementation Details.}

The \textit{Only-Transcription} condition was implemented as a controlled
textual setting designed to isolate the contribution of the linguistic
information derived from the audio channel. For each VSV item, the model
receives the audio transcription together with the two minimally contrastive
textual alternatives. No visual frames, video representation, or raw acoustic
signal are provided. The original question is retained as dataset metadata but
is not included in the inference prompt. The model must therefore discriminate
between caption and foil using only the textual content extracted from the
audio and the linguistic information contained in the two alternatives.

This condition differs from the \textit{No Input} setting in a single
controlled aspect. While the latter exposes the model only to the caption--foil
pair, \textit{Only-Transcription} additionally provides the textual
representation of the audio track. It therefore makes it possible to determine
whether speech-derived linguistic information contributes to the decision even
when direct perceptual access to the original video and acoustic signal is
removed.

As in the other experimental conditions, the evaluation logic is centralized
in a common module shared across the evaluated models. The system instruction,
candidate ordering, target representation, output parser, and result
serialization remain unchanged. Model-specific adapters are only responsible
for applying the appropriate chat template and performing generation.

The common system instruction is:

\begin{quote}
\small
\texttt{Sei un assistente binario per un task di Visual Statement Verification.}\\
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
\footnote{English translation: You are a binary assistant for a Visual
Statement Verification task. Respond exclusively with either 0 or 1.}

The condition-specific prompt is defined as follows:

\begin{quote}
\small
\texttt{Leggi esclusivamente la trascrizione dell'audio fornita.}\\
\texttt{Scegli quale delle due descrizioni è corretta sulla base}\\
\texttt{esclusivamente delle informazioni testuali disponibili nella}\\
\texttt{trascrizione.}\\[0.5em]
\texttt{Trascrizione dell'audio: \{transcript\}}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.\footnote{English translation: \textit{Read exclusively the provided audio
transcription. Choose which of the two descriptions is correct based
exclusively on the textual information available in the transcription.
Audio transcription: \{transcript\}. 0: \{answer1\}. 1: \{answer2\}.
Respond exclusively with either 0 or 1.}}}
\end{quote}


The transcription is not generated during VSV inference. Instead, the
previously generated transcriptions are stored in a dedicated CSV file and
loaded before the evaluation begins. The common implementation maps each
transcription to the corresponding video name:

\begin{lstlisting}[language=Python]
TRANSCRIPTIONS = Path(
    "data/input/transcription/transcriptions.csv"
)

def transcriptions():
    with TRANSCRIPTIONS.open(
        encoding="utf-8-sig",
        newline=""
    ) as f:
        return {
            r["video_name"]: r["transcription"]
            for r in csv.DictReader(f, delimiter=";")
        }
\end{lstlisting}

This mapping allows the same textual representation to be retrieved
consistently for every VSV item associated with the same video. During the
common evaluation loop, transcription retrieval is explicitly restricted to
the two conditions in which this information is required:

\begin{lstlisting}[language=Python]
transcript = (
    transcripts.get(row["video_name"], "")
    if mode in (
        "only_transcription",
        "transcript_video",
    )
    else ""
)
\end{lstlisting}

For \textit{Only-Transcription}, the retrieved text is inserted directly into
the condition-specific prompt. Prompt construction remains centralized and
independent of the model architecture:

\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}

The two candidate statements correspond to the minimally contrastive
caption--foil pair. Their numerical labels do not encode their semantic
status. The true caption may occupy either position and the dataset target
specifies whether the expected answer is \texttt{0} or \texttt{1}. The
transcription is therefore auxiliary evidence used to discriminate between
the same alternatives employed in every other experimental condition.

A relevant implementation property of this setting is the complete absence of
multimodal objects from the inference request. At adapter level,
\texttt{only\_transcription} does not trigger the loading of either visual or
acoustic resources. The general logic can be represented as follows:

\begin{lstlisting}[language=Python]
content = []

if mode == "only_transcription":
    pass

content.append({
    "type": "text",
    "text": prompt,
})
\end{lstlisting}

Consequently, the transcription is not processed through an audio encoder. It
is tokenized as ordinary textual input together with the task instruction and
the two candidate statements. This distinction is central to the experimental
design. The condition evaluates access to the semantic content extracted from
audio rather than access to the original acoustic modality itself. Prosodic
information, speaker characteristics, environmental sounds, and other
non-textual acoustic properties are therefore unavailable unless they were
explicitly represented in the generated transcription.

The same inference interface used by the other conditions is then called with
the formatted prompt:

\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)

pred = prediction(raw)
\end{lstlisting}

Although the common function also provides the media paths to the adapter,
none of these resources is included in the model input for
\textit{Only-Transcription}. This preserves a uniform inference interface
across conditions without altering the information available in the present
setting.

All models are required to return a binary response. The generated output is
processed through the shared parser:

\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(
        r"(?<!\d)[01](?!\d)",
        str(text),
    )
    return int(m.group()) if m else None
\end{lstlisting}

The parser extracts the first isolated occurrence of \texttt{0} or
\texttt{1}. If no valid binary label is found, the prediction is marked as
invalid rather than being assigned automatically to one of the alternatives.
Valid predictions are compared with the target associated with the
caption--foil pair:

\begin{lstlisting}[language=Python]
result = (
    "correct"
    if pred == int(row["target"])
    else "wrong"
    if pred is not None
    else "invalid"
)
\end{lstlisting}
Generation is performed deterministically, with stochastic sampling disabled,
and the maximum generated sequence is restricted to the few tokens required
for the binary response. This minimizes generation variability and keeps the
model output aligned with the discriminative nature of the VSV task.
For each item, the common evaluation pipeline stores the raw model output, the
parsed prediction, the expected target, the correctness status, the model
identifier, the experimental condition, and possible execution errors. Results
are written to a dedicated \texttt{only\_transcription} directory, with a
separate CSV file for each evaluated model. Previously completed item
identifiers are detected before inference, which allows interrupted executions
to resume without recomputing existing predictions.
The \textit{Only-Transcription} condition therefore provides a direct
diagnostic of the contribution of textualized audio information. Its comparison
with \textit{No Input} measures the additional contribution introduced by the
transcription itself. Its comparison with \textit{Audio-Only} distinguishes
the semantic information preserved by textualization from the information
available in the original acoustic signal. Finally, comparison with conditions
including visual evidence makes it possible to examine whether the same
linguistic information remains useful when it is combined with the primary
visual channel.


\subsubsection*{The Audio-Visual (AV) Condition}
The \textit{Audiovisual} condition represents the natural configuration of information available to the model, comprising both the visual sequence and the native acoustic signal from the original video. Unlike the \textit{Video + Transcription} condition described below, the input is provided as a single integrated stream. This serves as the core configuration for verifying whether the simultaneous presence of multiple modalities yields an advantage over unimodal conditions or leads to a \textit{unimodal collapse} phenomenon. Its analytical value extends beyond absolute performance, as it enables item-level observation of whether the acoustic channel repairs errors made in the \textit{Video Only} condition or, in the worst-case scenario, introduces new incorrect decisions.
\paragraph{Implementation Details}
The \textit{Audio + Video} condition represents the complete perceptual configuration of the experimental framework. For each VSV item, the model receives both the visual and acoustic information associated with the corresponding original video, together with the two minimally contrastive textual alternatives. Unlike the unimodal ablations, neither perceptual channel is removed. No audio-derived transcription is included in this condition. The original question is retained as dataset metadata and is not presented to the model during inference.
This configuration is designed to evaluate the model when the two perceptual sources naturally available in the original stimulus are simultaneously accessible. More specifically, it provides the reference condition for investigating whether acoustic information contributes additional evidence when visual information is already present. Its comparison with \textit{Video-Only} therefore isolates the effect of introducing the acoustic channel, while its comparison with \textit{Audio-Only} shows how performance changes when visual evidence is added to the acoustic signal.
As in the remaining experimental configurations, prompt construction and evaluation are centralized in the common pipeline and are independent of the specific model architecture. The system instruction remains unchanged:
\begin{quote}
\small
\texttt{Sei un assistente binario per un task di Visual Statement Verification.}\\
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
\footnote{English translation: You are a binary assistant for a Visual Statement Verification task. Respond exclusively with either 0 or 1.}
The condition-specific prompt is defined as follows:
\begin{quote}
\small
\texttt{Osserva il video e ascolta il relativo audio.}\\
\texttt{Scegli quale delle due descrizioni è corretta sulla base}\\
\texttt{dell'insieme delle informazioni visive e acustiche disponibili.}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
\footnote{English translation: Observe the video and listen to its audio. Choose which of the two descriptions is correct based on the combined visual and acoustic information available. 0: \{answer1\}. 1: \{answer2\}. Respond exclusively with either 0 or 1.}

The two alternatives correspond to the same minimally contrastive caption--foil pair used in all experimental conditions. The numerical position of the true caption is not fixed and is specified by the target label stored for each item. Consequently, the transition from a unimodal to a multimodal condition does not modify the decision problem itself. Only the perceptual evidence available to support that decision changes.

The common pipeline resolves three media resources for each item:

\begin{lstlisting}[language=Python]
def paths(row):
    stem = Path(row["video_name"]).stem
    return {
        "audio": AUDIO_DIR / f"{stem}.mp3",
        "mute": MUTE_DIR / f"{stem}.mp4",
        "video": VIDEO_DIR / f"{stem}.mp4",
    }
\end{lstlisting}

Unlike the \textit{Video-Only} condition, which relies on the muted version, the \textit{Audio + Video} condition uses the original audiovisual file identified by \texttt{paths["video"]}. This distinction ensures that both modalities originate from the same source stimulus.

When the architecture requires the acoustic stream to be supplied separately, the audio waveform is extracted directly from the same original MP4 file used for the visual channel. Audio extraction is centralized in the common pipeline:

\begin{lstlisting}[language=Python]
def extract_audio(video):
    out = (
        Path(tempfile.gettempdir())
        / "maia_av_audio_cache"
        / f"{video.stem}.wav"
    )

    out.parent.mkdir(exist_ok=True)

    if not out.exists():
        subprocess.run(
            [
                "ffmpeg",
                "-loglevel", "error",
                "-y",
                "-i", str(video),
                "-vn",
                "-ac", "1",
                "-ar", "16000",
                str(out),
            ],
            check=True,
        )

    return out
\end{lstlisting}

The audio stream is therefore converted to a mono waveform sampled at \(16\,\mathrm{kHz}\). Importantly, the waveform is not obtained from an independent textual representation or from a different recording: it is extracted from the same audiovisual file used as the source of the visual content. This preserves the correspondence between the two modalities at the stimulus level.

The general multimodal preparation performed by the model adapters can be abstracted as follows:

\begin{lstlisting}[language=Python]
if mode == "video_audio":
    video = load_video(paths["video"])

    audio = load_audio(
        extract_audio(paths["video"])
    )
\end{lstlisting}

For adapters that explicitly decode the visual stream, the video is represented using a fixed budget of 32 frames distributed over the complete duration of the clip:

\begin{lstlisting}[language=Python]
N_FRAMES = 32

frame_indices = np.unique(
    np.linspace(
        0,
        total_frames - 1,
        min(N_FRAMES, total_frames),
        dtype=int,
    )
)
\end{lstlisting}

Uniform sampling is used so that the visual representation covers the complete temporal extent of the stimulus rather than concentrating the available visual budget in a single segment. When fewer than 32 frames are available, all valid frames are retained.

The multimodal request then contains both perceptual streams. In adapters where the modalities are explicitly exposed to the inference backend, the general structure is:

\begin{lstlisting}[language=Python]
multi_modal_data = {}

if video is not None:
    multi_modal_data["video"] = video

if audio is not None:
    multi_modal_data["audio"] = (
        audio,
        AUDIO_SAMPLE_RATE,
    )

request["multi_modal_data"] = multi_modal_data
\end{lstlisting}

The exact representation passed to the model remains architecture-dependent. Some models accept the visual sequence and the acoustic waveform as two explicit multimodal inputs, while models providing native audiovisual video processing can recover the acoustic stream directly from the original video. These differences are confined to the model-specific adapters and do not alter the experimental condition: in every case, both visual and acoustic evidence originate from the same original stimulus and are jointly available during inference.

This distinction can be represented at adapter level through modality flags:

\begin{lstlisting}[language=Python]
use_video = mode in {
    "only_video",
    "video_audio",
    "transcript_video",
}

use_audio = mode in {
    "only_audio",
    "video_audio",
}
\end{lstlisting}

Thus, \texttt{video\_audio} is the only condition in which both flags are simultaneously active. This provides a direct implementation-level distinction between the joint audiovisual configuration and the corresponding unimodal ablations.

Prompt construction remains unchanged with respect to the remaining conditions:

\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}

No transcription is retrieved for \texttt{video\_audio}. The textual component of the request therefore contains exclusively the task instruction and the two candidate statements, while all additional evidence is supplied through the raw perceptual modalities.

The common evaluation loop subsequently delegates multimodal inference to the corresponding model adapter:

\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)

pred = prediction(raw)
\end{lstlisting}

All models are required to return the same binary decision independently of their internal multimodal interface. The resulting generation is normalized through the common parser:

\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(
        r"(?<!\d)[01](?!\d)",
        str(text),
    )
    return int(m.group()) if m else None
\end{lstlisting}

The first isolated occurrence of \texttt{0} or \texttt{1} is interpreted as the model prediction. If neither label can be extracted, the response is classified as invalid. Valid predictions are compared with the target label using the same procedure adopted across the entire experimental framework:

\begin{lstlisting}[language=Python]
result = (
    "correct"
    if pred == int(row["target"])
    else "wrong"
    if pred is not None
    else "invalid"
)
\end{lstlisting}

Inference is performed deterministically, with stochastic sampling disabled in the model-specific generation adapters. For every item, the common pipeline stores the raw output, parsed binary prediction, expected target, correctness status, model identifier, experimental mode, and possible execution errors. Results are stored in the dedicated \texttt{video\_audio} output directory using a separate CSV file for each model.

The \textit{Audio + Video} condition is therefore not treated as an independent task, but as the joint-modality counterpart of the two unimodal ablations. The caption--foil decision, target labels, prompt structure, prediction parser, and evaluation procedure remain fixed. The experimental manipulation concerns only the perceptual evidence made available to the model. In particular, comparison with \textit{Video-Only} provides a controlled measure of whether the addition of raw acoustic information improves, preserves, or interferes with decisions already supported by visual evidence. Conversely, comparison with the textualized audiovisual condition allows the contribution of native acoustic processing to be distinguished from the contribution of the linguistic information preserved in an audio transcription.
\subsubsection*{The Vision-Language (VL) Configuration}
The \textit{Video + Transcription} condition combines the visual component with a textual representation of speech. This serves as a second multimodal configuration, allowing us to compare direct audiovisual integration against textualized linguistic information. Comparing this setting with the \textit{Audiovisual} condition enables us to verify whether the informative content of the acoustic track is leveraged more efficiently when transformed into text. Furthermore, comparison with \textit{Video Only} clarifies the benefit derived from audio-based linguistic information when visual context is already available.
\paragraph{Implementation Details}

The \textit{Audio + Video} condition represents the complete perceptual configuration of the experimental framework. For each VSV item, the model receives both the visual and acoustic information associated with the corresponding original video, together with the two minimally contrastive textual alternatives. Unlike the unimodal ablations, neither perceptual channel is removed. No audio-derived transcription is included in this condition. The original question is retained as dataset metadata and is not presented to the model during inference.

This configuration is designed to evaluate the model when the two perceptual sources naturally available in the original stimulus are simultaneously accessible. More specifically, it provides the reference condition for investigating whether acoustic information contributes additional evidence when visual information is already present. Its comparison with \textit{Video-Only} therefore isolates the effect of introducing the acoustic channel, while its comparison with \textit{Audio-Only} shows how performance changes when visual evidence is added to the acoustic signal.

As in the remaining experimental configurations, prompt construction and evaluation are centralized in the common pipeline and are independent of the specific model architecture. The system instruction remains unchanged:

\begin{quote}
\small
\texttt{Sei un assistente binario per un task di Visual Statement Verification.}\\
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
\footnote{English translation: ``You are a binary assistant for a Visual Statement Verification task. Respond exclusively with either 0 or 1.''}

The condition-specific prompt is defined as follows:

\begin{quote}
\small
\texttt{Osserva il video e ascolta il relativo audio.}\\
\texttt{Scegli quale delle due descrizioni è corretta sulla base}\\
\texttt{dell'insieme delle informazioni visive e acustiche disponibili.}\\[0.5em]
\texttt{0: \{answer1\}}\\
\texttt{1: \{answer2\}}\\[0.5em]
\texttt{Rispondi esclusivamente con 0 oppure 1.}
\end{quote}
\footnote{English translation: ``Observe the video and listen to its audio. Choose which of the two descriptions is correct based on the combined visual and acoustic information available. 0: \{answer1\}. 1: \{answer2\}. Respond exclusively with either 0 or 1.''}

The two alternatives correspond to the same minimally contrastive caption--foil pair used in all experimental conditions. The numerical position of the true caption is not fixed and is specified by the target label stored for each item. Consequently, the transition from a unimodal to a multimodal condition does not modify the decision problem itself. Only the perceptual evidence available to support that decision changes.

The common pipeline resolves three media resources for each item:

\begin{lstlisting}[language=Python]
def paths(row):
    stem = Path(row["video_name"]).stem
    return {
        "audio": AUDIO_DIR / f"{stem}.mp3",
        "mute": MUTE_DIR / f"{stem}.mp4",
        "video": VIDEO_DIR / f"{stem}.mp4",
    }
\end{lstlisting}

Unlike the \textit{Video-Only} condition, which relies on the muted version, the \textit{Audio + Video} condition uses the original audiovisual file identified by \texttt{paths["video"]}. This distinction ensures that both modalities originate from the same source stimulus.

When the architecture requires the acoustic stream to be supplied separately, the audio waveform is extracted directly from the same original MP4 file used for the visual channel. Audio extraction is centralized in the common pipeline:

\begin{lstlisting}[language=Python]
def extract_audio(video):
    out = (
        Path(tempfile.gettempdir())
        / "maia_av_audio_cache"
        / f"{video.stem}.wav"
    )

    out.parent.mkdir(exist_ok=True)

    if not out.exists():
        subprocess.run(
            [
                "ffmpeg",
                "-loglevel", "error",
                "-y",
                "-i", str(video),
                "-vn",
                "-ac", "1",
                "-ar", "16000",
                str(out),
            ],
            check=True,
        )

    return out
\end{lstlisting}

The audio stream is therefore converted to a mono waveform sampled at \(16\,\mathrm{kHz}\). Importantly, the waveform is not obtained from an independent textual representation or from a different recording: it is extracted from the same audiovisual file used as the source of the visual content. This preserves the correspondence between the two modalities at the stimulus level.

The general multimodal preparation performed by the model adapters can be abstracted as follows:

\begin{lstlisting}[language=Python]
if mode == "video_audio":
    video = load_video(paths["video"])

    audio = load_audio(
        extract_audio(paths["video"])
    )
\end{lstlisting}

For adapters that explicitly decode the visual stream, the video is represented using a fixed budget of 32 frames distributed over the complete duration of the clip:

\begin{lstlisting}[language=Python]
N_FRAMES = 32

frame_indices = np.unique(
    np.linspace(
        0,
        total_frames - 1,
        min(N_FRAMES, total_frames),
        dtype=int,
    )
)
\end{lstlisting}

Uniform sampling is used so that the visual representation covers the complete temporal extent of the stimulus rather than concentrating the available visual budget in a single segment. When fewer than 32 frames are available, all valid frames are retained.

The multimodal request then contains both perceptual streams. In adapters where the modalities are explicitly exposed to the inference backend, the general structure is:

\begin{lstlisting}[language=Python]
multi_modal_data = {}

if video is not None:
    multi_modal_data["video"] = video

if audio is not None:
    multi_modal_data["audio"] = (
        audio,
        AUDIO_SAMPLE_RATE,
    )

request["multi_modal_data"] = multi_modal_data
\end{lstlisting}

The exact representation passed to the model remains architecture-dependent. Some models accept the visual sequence and the acoustic waveform as two explicit multimodal inputs, while models providing native audiovisual video processing can recover the acoustic stream directly from the original video. These differences are confined to the model-specific adapters and do not alter the experimental condition: in every case, both visual and acoustic evidence originate from the same original stimulus and are jointly available during inference.

This distinction can be represented at adapter level through modality flags:

\begin{lstlisting}[language=Python]
use_video = mode in {
    "only_video",
    "video_audio",
    "transcript_video",
}

use_audio = mode in {
    "only_audio",
    "video_audio",
}
\end{lstlisting}

Thus, \texttt{video\_audio} is the only condition in which both flags are simultaneously active. This provides a direct implementation-level distinction between the joint audiovisual configuration and the corresponding unimodal ablations.

Prompt construction remains unchanged with respect to the remaining conditions:

\begin{lstlisting}[language=Python]
def prompt(row, mode, transcript=""):
    return PROMPTS[mode].format(
        answer1=row["answer1"],
        answer2=row["answer2"],
        transcript=transcript,
    )
\end{lstlisting}

No transcription is retrieved for \texttt{video\_audio}. The textual component of the request therefore contains exclusively the task instruction and the two candidate statements, while all additional evidence is supplied through the raw perceptual modalities.

The common evaluation loop subsequently delegates multimodal inference to the corresponding model adapter:

\begin{lstlisting}[language=Python]
raw = infer(
    mode,
    row,
    prompt(row, mode, transcript),
    paths(row),
)

pred = prediction(raw)
\end{lstlisting}

All models are required to return the same binary decision independently of their internal multimodal interface. The resulting generation is normalized through the common parser:

\begin{lstlisting}[language=Python]
def prediction(text):
    m = re.search(
        r"(?<!\d)[01](?!\d)",
        str(text),
    )
    return int(m.group()) if m else None
\end{lstlisting}

The first isolated occurrence of \texttt{0} or \texttt{1} is interpreted as the model prediction. If neither label can be extracted, the response is classified as invalid. Valid predictions are compared with the target label using the same procedure adopted across the entire experimental framework:

\begin{lstlisting}[language=Python]
result = (
    "correct"
    if pred == int(row["target"])
    else "wrong"
    if pred is not None
    else "invalid"
)
\end{lstlisting}

Inference is performed deterministically, with stochastic sampling disabled in the model-specific generation adapters. For every item, the common pipeline stores the raw output, parsed binary prediction, expected target, correctness status, model identifier, experimental mode, and possible execution errors. Results are stored in the dedicated \texttt{video\_audio} output directory using a separate CSV file for each model.

The \textit{Audio + Video} condition is therefore not treated as an independent task, but as the joint-modality counterpart of the two unimodal ablations. The caption--foil decision, target labels, prompt structure, prediction parser, and evaluation procedure remain fixed. The experimental manipulation concerns only the perceptual evidence made available to the model. In particular, comparison with \textit{Video-Only} provides a controlled measure of whether the addition of raw acoustic information improves, preserves, or interferes with decisions already supported by visual evidence. Conversely, comparison with the textualized audiovisual condition allows the contribution of native acoustic processing to be distinguished from the contribution of the linguistic information preserved in an audio transcription.
\subsection*{Metrics Selection and Aggregation Strategy }
\subsubsection*{Linguistic and Structural Bias Assessment under Zero-Input}
The \textit{No Input} condition requires distinct analytical treatment compared to other performance metrics. Its primary purpose is to measure the extent to which the task can be solved without perceptual stimuli, making it unsuitable for direct inclusion in standard performance comparisons. Instead, dedicated metrics are employed to quantify \textbf{linguistic and structural shortcuts}.
The \textit{No Input} condition is designed to quantify the extent to which a model can solve the VSV task without access to perceptual evidence. Since neither visual nor acoustic information is available, performance in this setting cannot be interpreted as evidence of multimodal understanding. Instead, the analysis focuses on whether the model can exploit linguistic regularities, structural properties of the alternatives, or systematic biases in the dataset.

Let \(N\) denote the number of evaluated items. For each item \(i\), let \(y_i \in \{0,1\}\) be the target label and \(\hat{y}_i \in \{0,1\}\) the prediction produced by the model.
\begin{description}
    \item[Raw Accuracy]\\
The first measure is \textit{Raw Accuracy}, defined as the proportion of items for which the predicted label corresponds to the target:
\begin{equation}
\operatorname{Acc}_{\text{NoInput}} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}\left(\hat{y}_i = y_i\right),
\end{equation}
where \(\mathbb{I}(\cdot)\) is the indicator function, equal to \(1\) when the condition is satisfied and \(0\) otherwise.
Because each item requires a binary choice between two alternatives, a model selecting one of the two options uniformly at random has expected accuracy
\begin{equation}
\mathbb{E}\left[\operatorname{Acc}_{\text{random}}\right] = \frac{1}{2} = 0.5.
\end{equation}
An accuracy close to \(0.5\) is therefore compatible with behavior close to random guessing. Conversely, a substantially higher value indicates that the model can identify the target more frequently than expected by chance despite the absence of perceptual evidence. This suggests that some information contained in the linguistic form of the alternatives may be predictive of the correct answer.
Raw Accuracy must not be interpreted as a measure of language comprehension. The model is not required to understand the underlying video event and does not have access to the evidence necessary to perform grounded reasoning. The metric only measures its ability to recover the target from the two textual alternatives available in the \textit{No Input} condition.
\item[Language-Only Advantage]\\
To express the deviation from the random baseline more directly, we compute the \textit{Language-Only Advantage} (LOA). It is obtained by subtracting the expected binary chance level from the observed \textit{No Input} accuracy:
\begin{equation}
\operatorname{LOA} = \operatorname{Acc}_{\text{NoInput}} - 0.5.
\end{equation}
The transformation can be derived directly from the decomposition
\begin{equation}
\operatorname{Acc}_{\text{NoInput}} = 0.5 + \operatorname{LOA}.
\end{equation}
Hence, \(\operatorname{LOA}=0\) indicates performance exactly at the random baseline. Positive values indicate performance above chance:
\begin{equation}
\operatorname{LOA} > 0 \quad \Longrightarrow \quad \operatorname{Acc}_{\text{NoInput}} > 0.5,
\end{equation}
while negative values indicate performance below chance:
\begin{equation}
\operatorname{LOA} < 0 \quad \Longrightarrow \quad \operatorname{Acc}_{\text{NoInput}} < 0.5.
\end{equation}
The main advantage of this transformation is interpretability. Rather than reporting the complete accuracy value, LOA directly expresses the amount of performance exceeding the expected random level. It can therefore be used as a compact indicator of the potential contribution of linguistic or structural shortcuts.
A positive LOA does not identify the nature of the shortcut. It only establishes that the textual alternatives contain sufficient regularities for the model to perform above chance. Additional diagnostics are therefore required to determine which properties may contribute to this effect.
\item[Matthews Correlation Coefficient]\\
Accuracy alone can conceal degenerate decision strategies. For example, a model may develop a systematic preference for one output label and still obtain a non-negligible accuracy, particularly when the observed predictions are not evenly distributed across the two positions.
For this reason, the analysis also includes the \textit{Matthews Correlation Coefficient} (MCC). For the binary task, predictions can be summarized through the four entries of the confusion matrix: true positives (\(\mathrm{TP}\)), true negatives (\(\mathrm{TN}\)), false positives (\(\mathrm{FP}\)), and false negatives (\(\mathrm{FN}\)). MCC is defined as
\begin{equation}
\operatorname{MCC} = \frac{\mathrm{TP} \cdot \mathrm{TN} - \mathrm{FP} \cdot \mathrm{FN}}{\sqrt{(\mathrm{TP}+\mathrm{FP})(\mathrm{TP}+\mathrm{FN})(\mathrm{TN}+\mathrm{FP})(\mathrm{TN}+\mathrm{FN})}}.
\end{equation}
The numerator compares concordant and discordant predictions. Correct positive and negative decisions contribute through \(\mathrm{TP} \cdot \mathrm{TN}\), while the two types of error contribute through \(\mathrm{FP} \cdot \mathrm{FN}\). The denominator normalizes this difference according to the marginal distributions of targets and predictions.
MCC ranges between \(-1\) and \(1\):
\begin{equation}
-1 \leq \operatorname{MCC} \leq 1.
\end{equation}
A value close to \(1\) indicates strong agreement between predictions and targets. A value close to \(0\) indicates little or no association. A negative value indicates a systematic inverse association.
MCC complements Raw Accuracy because it evaluates the overall correspondence between predictions and targets while taking all four components of the confusion matrix into account. It is therefore useful for distinguishing genuine target-sensitive performance from apparently successful behavior produced by strongly unbalanced prediction strategies.
\item[Position Bias]\\
The \textit{Position Bias} analysis directly measures whether the model systematically prefers one of the two candidate positions independently of their content.
Let
\begin{equation}
p_0 = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}\left(\hat{y}_i = 0\right)
\end{equation}
denote the empirical probability of selecting the first alternative, and
\begin{equation}
p_1 = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}\left(\hat{y}_i = 1\right) = 1-p_0
\end{equation}
the probability of selecting the second alternative.
Under perfectly balanced selection behavior,
\begin{equation}
p_0 = p_1 = 0.5.
\end{equation}
We therefore define the magnitude of the positional deviation as
\begin{equation}
\operatorname{PB} = \left|p_0 - 0.5\right|.
\end{equation}
Because \(p_1=1-p_0\), the same quantity can equivalently be written as
\begin{equation}
\operatorname{PB} = \left|p_1 - 0.5\right|.
\end{equation}
The preferred position is defined as
\begin{equation}
\operatorname{PreferredPosition} = \operatorname*{argmax}_{j \in \{0,1\}} p_j.
\end{equation}
A Position Bias equal to \(0\) indicates perfectly balanced selection frequencies. The maximum possible value is \(0.5\), obtained when the model always selects the same position:
\begin{equation}
0 \leq \operatorname{PB} \leq 0.5.
\end{equation}
This diagnostic is required because above-chance Raw Accuracy does not by itself demonstrate the exploitation of meaningful linguistic information. Part of the observed result may instead originate from a systematic preference for the first or second alternative. Position Bias therefore provides a direct measure of this potential confound.
\item[Lexical Attraction]\\
The \textit{Lexical Attraction} analysis evaluates whether the model preferentially selects the alternative that is lexically more similar to the original question.
For each item \(i\), let \(q_i\) denote the question and let \(a_{i0}\) and \(a_{i1}\) denote the two textual alternatives. Each text is represented as a TF--IDF vector. The cosine similarity between the question and alternative \(j\) is defined as
\begin{equation}
s_{ij} = \cos \left(\mathbf{v}_{q_i}, \mathbf{v}_{a_{ij}}\right) = \frac{\mathbf{v}_{q_i}^{\top} \mathbf{v}_{a_{ij}}}{\left\|\mathbf{v}_{q_i}\right\|_2 \left\|\mathbf{v}_{a_{ij}}\right\|_2}, \qquad j \in \{0,1\},
\end{equation}
where \(\mathbf{v}_{q_i}\) and \(\mathbf{v}_{a_{ij}}\) are the corresponding TF--IDF representations.
The lexically closest alternative is then identified as
\begin{equation}
\ell_i = \operatorname*{argmax}_{j \in \{0,1\}} s_{ij}.
\end{equation}
The \textit{Lexical Attraction Rate} measures how frequently the model selects this alternative:
\begin{equation}
\operatorname{LAR} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I} \left(\hat{y}_i = \ell_i\right).
\end{equation}
A high value indicates that the model tends to prefer the candidate sharing greater lexical similarity with the question.
However, this behavioral tendency does not establish that lexical overlap represents a dataset bias. To evaluate the structural relationship between lexical similarity and the target, a second quantity is computed:
\begin{equation}
\operatorname{LTA} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I} \left(y_i = \ell_i\right),
\end{equation}
where \(\operatorname{LTA}\) denotes the \textit{Lexical Target Alignment}. It measures how frequently the correct alternative is also the one with the highest lexical similarity to the question.
The two measures therefore capture different phenomena:
\begin{align}
\operatorname{LAR} &\rightarrow \text{model behavior}, \\
\operatorname{LTA} &\rightarrow \text{dataset structure}.
\end{align}
This distinction is essential. A high Lexical Attraction Rate means that the model follows lexical similarity, but this strategy is only useful if lexical similarity is correlated with the target. Conversely, a high Lexical Target Alignment indicates that such a shortcut is available in the dataset, even if a specific model does not exploit it systematically.
The interaction between the two quantities can therefore be interpreted as follows:
\begin{equation}
\operatorname{LAR} \gg 0.5 \quad \text{and} \quad \operatorname{LTA} \gg 0.5
\end{equation}
provides evidence for both the availability of a lexical shortcut and the tendency of the model to exploit it.
\item[Length Bias]\\
A parallel analysis is performed for the length of the two alternatives. Let \(L(a_{ij})\) denote the length assigned to alternative \(j\) for item \(i\), according to the length representation adopted in the implementation. The longer candidate is identified as
\begin{equation}
h_i = \operatorname*{argmax}_{j \in \{0,1\}} L(a_{ij}).
\end{equation}
The model's preference for longer alternatives is measured through the \textit{Length Attraction Rate}:
\begin{equation}
\operatorname{LenAR} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I} \left(\hat{y}_i = h_i\right).
\end{equation}
This quantity describes a behavioral property of the model. Values substantially above \(0.5\) indicate a tendency to select the longer alternative.
As for lexical similarity, a separate measure is required to determine whether this heuristic could actually be rewarded by the dataset. We therefore compute the \textit{Length Target Alignment}:
\begin{equation}
\operatorname{LenTA} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I} \left(y_i = h_i\right).
\end{equation}
This metric measures how frequently the correct alternative is also the longer one.
The distinction again separates model behavior from dataset structure:
\begin{align}
\operatorname{LenAR} &\rightarrow \text{preference of the model for longer alternatives}, \\
\operatorname{LenTA} &\rightarrow \text{association between alternative length and the target}.
\end{align}
A model may prefer longer alternatives even when length contains no useful information about the correct label. Conversely, the dataset may contain a correlation between length and correctness that is not exploited by a particular model. Considering both quantities prevents these two cases from being conflated.
\item[No-Input Category Breakdown]\\
The main \textit{No Input} diagnostics are additionally computed separately for each reasoning category. Let \(c\) denote a category and let
\begin{equation}
I_c = \left\{i \mid \operatorname{category}(i)=c\right\}
\end{equation}
be the subset of items belonging to that category, with \(N_c = |I_c|\).
Category-specific Raw Accuracy is therefore
\begin{equation}
\operatorname{Acc}_{c} = \frac{1}{N_c} \sum_{i \in I_c} \mathbb{I} \left(\hat{y}_i = y_i\right),
\end{equation}
and the corresponding Language-Only Advantage becomes
\begin{equation}
\operatorname{LOA}_{c} = \operatorname{Acc}_{c} - 0.5.
\end{equation}
MCC and Position Bias are computed analogously by restricting the confusion matrix and the prediction frequencies to the subset \(I_c\). In particular,
\begin{equation}
p_{0,c} = \frac{1}{N_c} \sum_{i \in I_c} \mathbb{I} \left(\hat{y}_i=0\right),
\end{equation}
with category-specific Position Bias
\begin{equation}
\operatorname{PB}_{c} = \left|p_{0,c}-0.5\right|.
\end{equation}
\end{description}
This decomposition makes it possible to determine whether \textit{No Input} solvability varies systematically across reasoning categories.
A higher \textit{No Input} accuracy for a given category must not be interpreted as evidence that the model is more competent at the corresponding reasoning process. In this experimental condition, the perceptual evidence required to perform spatial, temporal, causal, or other forms of grounded reasoning is unavailable. Category-level differences instead indicate that some groups of items may expose stronger linguistic or structural regularities than others.
This distinction is particularly important when the \textit{No Input} results are later compared with performance under perceptual conditions. For example, differences observed between spatial, temporal, and causal questions should not automatically be attributed to multimodal reasoning ability. Part of the variation may already be present before any perceptual modality is introduced. The category breakdown therefore provides a necessary baseline for separating genuine grounding effects from differences induced by the linguistic structure of the task.
\subsubsection*{}



%Non Corretto 













% %BOZZA IN ITALIANO 
% Performance Metrics
% Item Accuracy
% L'Item Accuracy costituisce la misura di performance più granulare per le condizioni nelle quali viene fornita informazione percettiva o trascritta. Essa corrisponde alla percentuale di singole decisioni caption–foil risolte correttamente.
% Questa metrica descrive la probabilità che il modello individui l'alternativa corretta su un singolo item. È inoltre la base utilizzata per i successivi confronti paired, nei quali viene osservato il cambiamento della medesima decisione tra due condizioni.
% Aggregate or Pool Accuracy
% La Pool Accuracy introduce un criterio più severo. Nell'attuale protocollo, un pool viene considerato valido soltanto quando contiene tutti e quattro gli item attesi. Il pool viene poi classificato come corretto esclusivamente se tutte le decisioni al suo interno sono corrette:
% [
% PoolCorrect = 1
% \quad \Longleftrightarrow \quad
% \forall i \in pool,\ correct_i = 1
% ]
% Una singola risposta errata rende quindi errato l'intero pool.
% Questa metrica misura una forma di consistenza più forte rispetto all'Item Accuracy. Una domanda non viene considerata risolta sulla base di un successo parziale. Il modello deve mantenere una decisione corretta attraverso tutte le verifiche associate alla stessa unità concettuale.
% Modality Contribution Metrics
% Conditional Modality Gain
% Il Conditional Modality Gain misura la variazione di performance prodotta dall'aggiunta di una nuova sorgente informativa. I confronti principali sono Video Only → Audio + Video e Video Only → Video + Transcription.
% Per una modalità aggiunta (M), il gain è definito come:
% [
% \Delta_M = Accuracy_{new} - Accuracy_{base}
% ]
% La metrica viene calcolata sugli stessi item disponibili in entrambe le condizioni. Il suo significato non consiste soltanto nel verificare se una configurazione multimodale presenti un'accuracy maggiore. Il confronto misura quanto cambino le decisioni quando alla stessa evidenza di base viene aggiunta una nuova modalità.
% Questo rappresenta uno dei punti di continuità più diretti con il principio sperimentale adottato in Lost in Transcription.
% Repair Rate
% Il Repair Rate considera esclusivamente gli item errati nella condizione di base e misura la proporzione di quelli che diventano corretti dopo l'aggiunta della nuova modalità:
% [
% RepairRate = P(New=1 \mid Base=0)
% ]
% La metrica identifica quindi i casi nei quali l'informazione aggiuntiva svolge una funzione correttiva.
% Questo comportamento è particolarmente rilevante per l'analisi della complementarità multimodale. Se il modello fallisce con la sola visione e riesce dopo l'introduzione dell'audio, la nuova modalità ha modificato positivamente una decisione precedentemente errata. Tale dinamica fornisce un'evidenza più informativa del semplice incremento medio di accuracy.
% Damage Rate
% Il Damage Rate rappresenta il comportamento opposto:
% [
% DamageRate = P(New=0 \mid Base=1)
% ]
% La metrica considera gli item corretti nella condizione di base e misura quanti diventino errati dopo l'aggiunta della nuova sorgente.
% Questo fenomeno permette di osservare forme di cross-modal interference. Una modalità supplementare può contenere informazione non pertinente, ambigua o difficile da integrare e può quindi compromettere una decisione precedentemente corretta.
% Per questa ragione il gain netto non è sufficiente a descrivere il comportamento del modello. Dieci repair accompagnati da dieci damage producono un delta complessivo nullo, ma rappresentano una dinamica molto diversa rispetto a una situazione nella quale nessuna decisione cambia. L'analisi viene quindi completata distinguendo stable correct, repair, damage e stable wrong.
% Audio-Video Interaction Effect
% Factorial Interaction Analysis
% L'interazione tra audio e video viene analizzata attraverso un design fattoriale (2 \times 2):

% [
% \begin{array}{c|cc}
% & V=0 & V=1\
% \hline
% A=0 & No\ Input & Video\ Only\
% A=1 & Audio\ Only & Audio+Video
% \end{array}
% ]

% In questo contesto No Input viene reintrodotto per una ragione strettamente statistica. Non rappresenta una condizione di comprensione multimodale, ma la cella necessaria a descrivere la situazione nella quale né audio né video sono presenti.

% La probabilità di una risposta corretta viene modellata attraverso:

% [
% logit(P(correct)) =
% \beta_0 +
% \beta_A A +
% \beta_V V +
% \beta_{AV}(A \times V)
% ]

% L'interesse principale riguarda il coefficiente (\beta_{AV}). Il termine quantifica se l'effetto prodotto dalla presenza simultanea di audio e video differisca da quanto atteso considerando separatamente i contributi delle due modalità.

% Un'interazione diversa da zero suggerisce che la combinazione audiovisiva non possa essere descritta come una semplice somma di effetti indipendenti. In presenza di un'interazione positiva, l'effetto congiunto risulta superiore a quanto previsto dai contributi separati. Un'interazione negativa indica invece una combinazione meno efficace rispetto all'attesa additiva sul piano del logit.

% L'analisi riporta coefficiente, standard error, statistica (z), (p)-value, intervallo di confidenza al 95%, odds ratio e relativo intervallo. Gli errori standard vengono calcolati in forma cluster-robust a livello di pool_id. Vengono inclusi soltanto gli item disponibili in tutte e quattro le celle del design, così da preservare la comparabilità paired necessaria all'interpretazione dell'interazione.

% Joint-Modality Evaluation
% Joint-Modality Accuracy

% La Joint-Modality Accuracy utilizza la classificazione delle domande per concentrare l'analisi sugli item nei quali una specifica combinazione di modalità è stata identificata come necessaria.

% L'accuracy generale di Audio + Video risponde alla domanda relativa alla performance complessiva della configurazione audiovisiva. La Joint-Modality Accuracy considera invece soltanto gli item classificati come dipendenti congiuntamente da audio e visione. Un principio analogo viene applicato agli item associati alla combinazione video e trascrizione.

% La differenza è metodologicamente importante. Un'elevata performance multimodale su item risolvibili con una sola modalità non dimostra capacità di integrazione. Concentrando la valutazione sui casi nei quali entrambe le sorgenti sono richieste, la metrica si avvicina maggiormente all'oggetto scientifico centrale dell'analisi.

% Alignment Sensitivity

% L'infrastruttura delle metriche prevede anche la possibilità di confrontare condizioni multimodali correttamente allineate con configurazioni nelle quali audio e video, oppure video e trascrizione, non corrispondono allo stesso contenuto.

% La Alignment Sensitivity è definita come:

% [
% AlignmentSensitivity =
% Accuracy_{aligned} -
% Accuracy_{misaligned}
% ]

% La misura permetterebbe di distinguere la semplice disponibilità simultanea di più modalità dalla capacità di utilizzare la loro corretta corrispondenza. Un modello realmente sensibile all'allineamento dovrebbe mostrare una degradazione quando le sorgenti vengono associate in modo scorretto.

% Questa analisi deve però essere inclusa nei main experiments soltanto nel caso in cui le condizioni misaligned siano state effettivamente eseguite nel protocollo finale. Se tali condizioni rimangono soltanto supportate dall'infrastruttura software, è più corretto presentare l'Alignment Sensitivity come estensione sperimentale possibile e non come parte dei risultati principali.

% Integration with the Preliminary Analysis
% Independent Visual-Evidence Estimation

% La preliminary analysis svolge una funzione distinta dai main experiments. La sua costruzione è indipendente dalle decisioni sperimentali e parte dal solo contenuto visivo. L'obiettivo è ottenere una rappresentazione strutturata di ciò che il modello riesce a estrarre dal video prima di confrontare tale rappresentazione con le informazioni richieste dagli item.

% La pipeline analizza progressivamente entità, eventi e relazioni inferenziali. L'informazione estratta viene poi confrontata con il contenuto necessario alla domanda attraverso una fase di Natural Language Inference. Il risultato costituisce una stima indipendente della disponibilità dell'evidenza visiva.

% Questa analisi non stabilisce direttamente se il modello risponderà correttamente nel task VSV. Stabilisce piuttosto se la rappresentazione ottenuta dal solo video contenga gli elementi necessari per affrontare la domanda. La distinzione tra disponibilità dell'informazione e comportamento osservato costituisce il fondamento della successiva integrazione.

% Mapping Preliminary and Experimental Outcomes

% I risultati della preliminary analysis vengono associati agli item dei main experiments attraverso gli identificativi condivisi. In questo modo, per ogni domanda è possibile mettere in relazione tre livelli di informazione.

% Il primo riguarda la disponibilità dell'evidenza visiva stimata indipendentemente. Il secondo riguarda la possibilità di risolvere il task senza percepire lo stimolo, misurata attraverso No Input. Il terzo corrisponde alla risposta effettivamente prodotta dal modello nelle diverse configurazioni modali.

% Le classi S0, S1 e le eventuali ulteriori classi definite dalla tassonomia rappresentano quindi una variabile di stratificazione della performance. Non sono derivate dalla correttezza del main experiment e possono essere utilizzate per interpretare la natura della decisione osservata.

% Diagnostic Rationale
% Why Preliminary Analysis and Main Experiments Must Be Combined

% L'accuracy da sola non consente di determinare la causa di una risposta corretta. Un modello può selezionare l'alternativa appropriata perché ha identificato l'evidenza richiesta nel video, perché ha sfruttato un pattern linguistico, perché ha prodotto un'inferenza plausibile oppure per effetto del caso.

% La preliminary analysis introduce una stima indipendente della presenza dell'evidenza visiva. No Input introduce una seconda diagnostica relativa alla risolvibilità dell'item in assenza dello stimolo. I main experiments mostrano invece il comportamento effettivo del sistema quando cambia l'informazione disponibile.

% L'integrazione delle tre componenti permette quindi di passare da una valutazione puramente prestazionale a una lettura diagnostica del comportamento. La correttezza diventa un dato da interpretare alla luce dell'evidenza teoricamente disponibile e degli shortcut osservabili.

% Interpretive Behavioural Patterns
% Visually Sufficient and Video-Only Correct

% Quando la preliminary analysis indica che l'informazione visiva è sufficiente e il modello risponde correttamente in Video Only, il comportamento è coerente con un'ipotesi di visual grounding. La risposta non dimostra direttamente il processo interno seguito dal modello, ma esiste una corrispondenza tra informazione disponibile e comportamento osservato.

% Visually Insufficient and No-Input Correct

% Quando l'evidenza visiva risulta insufficiente e il modello risponde correttamente anche in No Input, la decisione costituisce un forte candidato a una spiegazione basata su plausibilità linguistica o shortcut strutturali. In questo caso una risposta corretta in Video Only non può essere utilizzata come prova sufficiente di comprensione visiva.

% Visually Insufficient, Video-Only Wrong and Audio-Video Correct

% Questo pattern rappresenta uno dei casi più informativi. La preliminary analysis suggerisce che la sola visione non contenga evidenza sufficiente. Il modello fallisce effettivamente in Video Only e passa alla risposta corretta quando viene aggiunto l'audio.

% La sequenza è compatibile con un vero multimodal repair, poiché la nuova modalità interviene precisamente in una situazione nella quale l'evidenza visiva risulta carente.

% Video-Only Correct and Audio-Video Wrong

% Una risposta corretta con il solo video seguita da un errore dopo l'introduzione dell'audio costituisce un caso di possibile cross-modal interference. L'aggiunta della modalità non produce soltanto un mancato beneficio, ma altera negativamente una decisione già corretta.

% Visually Sufficient and Video-Only Wrong

% Se la preliminary analysis indica la presenza di evidenza sufficiente ma il modello fallisce in Video Only, l'errore non può essere spiegato semplicemente con l'assenza dell'informazione. Il contenuto necessario risultava teoricamente recuperabile, ma il modello non è riuscito a utilizzarlo efficacemente nel task.

% Questa configurazione permette di distinguere un limite di disponibilità dell'evidenza da un limite di sfruttamento dell'evidenza disponibile.

% Visually Insufficient, Video-Only Correct and No-Input Wrong

% Questo caso richiede un'interpretazione più prudente. Il modello risponde correttamente attraverso il video, ma la preliminary analysis non identifica evidenza sufficiente e No Input non offre una spiegazione linguistica immediata.

% La risposta può indicare la presenza di informazione visiva residua non catturata dalla pipeline preliminare. Può anche derivare da una strategia non descritta dalle diagnostiche disponibili. Il caso deve quindi essere considerato informativo proprio perché segnala una discrepanza tra i diversi livelli di analisi.

% Visually Insufficient, Video-Only Correct and No-Input Correct

% Quando sia Video Only sia No Input producono la risposta corretta nonostante una stima di insufficienza visiva, la correttezza nella condizione visuale non costituisce una prova affidabile di grounding. La spiegazione linguistica rimane compatibile con l'outcome osservato.

% Questi pattern mostrano perché la sola accuratezza non sia sufficiente per valutare l'integrazione multimodale. Una risposta corretta assume un significato diverso a seconda della disponibilità indipendente dell'evidenza e della possibilità di risolvere l'item senza percepire lo stimolo.

% Stratified Evaluation
% Preliminary-Analysis-Based Metrics

% L'integrazione tra preliminary analysis e main experiments consente di stratificare le metriche sperimentali secondo le classi di sufficienza definite dalla pipeline.

% Per ciascuna classe possono essere calcolate Item Accuracy e Pool Accuracy. Gli stessi gruppi possono essere utilizzati per stimare Conditional Modality Gain, Repair Rate e Damage Rate. È inoltre possibile osservare la distribuzione delle classi nelle categorie spatial, temporal e causal e verificare se la Joint-Modality Accuracy cambi in funzione della disponibilità dell'evidenza visiva.

% Questa stratificazione permette di formulare una domanda più informativa rispetto al semplice confronto tra Audio + Video e Video Only. L'interesse non consiste soltanto nel verificare se la configurazione multimodale migliori mediamente la performance. Diventa possibile verificare se il miglioramento avvenga precisamente negli item nei quali una stima indipendente suggerisce che la sola visione non fornisca informazione sufficiente.

% Un aumento concentrato in tali casi fornirebbe un'evidenza più forte a favore della complementarità multimodale rispetto a un incremento distribuito indiscriminatamente sull'intero dataset.

% Methods and Results Separation
% Methodological Scope

% La sezione Materials and Methods deve descrivere la funzione delle condizioni sperimentali, la modalità con cui vengono implementate, le metriche utilizzate, i confronti previsti e i criteri di inclusione degli item. Deve inoltre formalizzare il rapporto tra preliminary analysis, No Input e main experiments.

% I risultati numerici e le differenze osservate tra modelli devono rimanere separati. Affermazioni relative alla superiorità di un modello, alla maggiore performance di una categoria o all'entità di un gain appartengono alla sezione Results.

% È possibile introdurre aspettative teoriche quando utili alla motivazione del protocollo. Tali aspettative devono però essere formulate come ipotesi e mantenute chiaramente distinte dai risultati empirici.

% Overall Methodological Structure

% La struttura metodologica risultante segue una progressione precisa. No Input verifica se il task possa essere risolto senza percepire lo stimolo. La Preliminary Analysis stima se l'informazione richiesta sia recuperabile dal solo contenuto visivo. I Main Experiments osservano ciò che il modello effettivamente fa quando vengono rese disponibili differenti sorgenti di evidenza. La Cross-Analysis mette infine in relazione questi livelli per stabilire se una risposta corretta sia compatibile con grounding unimodale, integrazione multimodale o sfruttamento di shortcut.

% In questa prospettiva, MAIA-AV non viene ridotto a un confronto tra accuracy ottenute in sei condizioni. L'oggetto dell'analisi diventa il modo in cui la disponibilità dell'informazione modifica la decisione del modello. Il passaggio metodologico segue quindi una sequenza che procede da bias a evidence availability, da questa all'observed behaviour, fino alla valutazione della multimodal integration. Tale organizzazione permette di distinguere la presenza di informazione dalla capacità di utilizzarla e, soprattutto, impedisce di assumere che una risposta corretta costituisca automaticamente una prova di grounding.